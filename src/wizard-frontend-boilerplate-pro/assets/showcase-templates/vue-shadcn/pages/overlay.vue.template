<script setup lang="ts">
import { ref } from "vue";

import { Button } from "@/components/ui/button";
import { CodeBlock } from "@/components/ui/code-block";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover";
import {
  Sheet,
  SheetClose,
  SheetContent,
  SheetDescription,
  SheetFooter,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@/components/ui/sheet";

const POPOVER_CODE = `<script setup lang="ts">
import { ref } from "vue";
import { Button } from "@/components/ui/button";
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover";

const popoverOpen = ref(false);
<\/script>

<template>
  <Popover :open="popoverOpen" @openChange="val => (popoverOpen = val)">
    <PopoverTrigger as-child>
      <Button variant="outline" @click="popoverOpen = true">Open Popover</Button>
    </PopoverTrigger>
    <PopoverContent>
      <p class="text-sm text-muted-foreground">
        This is a popover. It can contain any content.
      </p>
      <Button class="mt-3 w-full" size="sm" variant="outline" @click="popoverOpen = false">
        Close
      </Button>
    </PopoverContent>
  </Popover>
</template>`;

const DROPDOWN_CODE = `<script setup lang="ts">
import { Button } from "@/components/ui/button";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
<\/script>

<template>
  <DropdownMenu>
    <DropdownMenuTrigger as-child>
      <Button variant="outline">Options</Button>
    </DropdownMenuTrigger>
    <DropdownMenuContent>
      <DropdownMenuLabel>My Account</DropdownMenuLabel>
      <DropdownMenuSeparator />
      <DropdownMenuItem>Profile</DropdownMenuItem>
      <DropdownMenuItem>Settings</DropdownMenuItem>
      <DropdownMenuItem>Billing</DropdownMenuItem>
    </DropdownMenuContent>
  </DropdownMenu>
</template>`;

const SHEET_CODE = `<script setup lang="ts">
import { ref } from "vue";
import { Button } from "@/components/ui/button";
import {
  Sheet,
  SheetClose,
  SheetContent,
  SheetDescription,
  SheetFooter,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@/components/ui/sheet";

const sheetOpen = ref(false);
<\/script>

<template>
  <Sheet :open="sheetOpen" @openChange="val => (sheetOpen = val)">
    <SheetTrigger as-child>
      <Button variant="outline" @click="sheetOpen = true">Open Sheet</Button>
    </SheetTrigger>
    <SheetContent side="right">
      <SheetHeader>
        <SheetTitle>Edit profile</SheetTitle>
        <SheetDescription>
          Make changes to your profile here. Click save when you're done.
        </SheetDescription>
      </SheetHeader>
      <div class="py-6">
        <p class="text-sm text-muted-foreground">Sheet content goes here.</p>
      </div>
      <SheetFooter>
        <SheetClose as-child>
          <Button variant="outline" @click="sheetOpen = false">Close</Button>
        </SheetClose>
      </SheetFooter>
    </SheetContent>
  </Sheet>
</template>`;

const dropdownOpen = ref(false);
const popoverOpen = ref(false);
const sheetOpen = ref(false);
</script>

<template>
  <div class="flex flex-col gap-16">
    <div>
      <h1 class="text-3xl font-bold tracking-tight text-foreground">Overlay</h1>
      <p class="mt-2 text-muted-foreground">
        Floating UI components that render above the main content — popovers, dropdown menus, and
        sheets.
      </p>
    </div>

    <section class="flex flex-col gap-4" id="popover">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Popover</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          A non-modal floating panel anchored to a trigger element, dismissed by clicking outside or
          pressing Escape.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <Popover :open="popoverOpen" @openChange="val => (popoverOpen = val)">
          <PopoverTrigger as-child>
            <Button variant="outline" @click="popoverOpen = true">Open Popover</Button>
          </PopoverTrigger>
          <PopoverContent>
            <p class="text-sm text-muted-foreground">
              This is a popover. It can contain any content.
            </p>
            <Button
              class="mt-3 w-full"
              size="sm"
              variant="outline"
              @click="popoverOpen = false"
            >
              Close
            </Button>
          </PopoverContent>
        </Popover>
      </div>
      <CodeBlock
        :code="POPOVER_CODE"
        :collapsible="true"
        :default-open="false"
        filename="Popover.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="dropdown-menu">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">DropdownMenu</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          A contextual menu with keyboard navigation, separators, and support for labels and nested
          items.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <DropdownMenu :open="dropdownOpen" @openChange="val => (dropdownOpen = val)">
          <DropdownMenuTrigger as-child>
            <Button variant="outline">Options</Button>
          </DropdownMenuTrigger>
          <DropdownMenuContent>
            <DropdownMenuLabel>My Account</DropdownMenuLabel>
            <DropdownMenuSeparator />
            <DropdownMenuItem>Profile</DropdownMenuItem>
            <DropdownMenuItem>Settings</DropdownMenuItem>
            <DropdownMenuItem>Billing</DropdownMenuItem>
          </DropdownMenuContent>
        </DropdownMenu>
      </div>
      <CodeBlock
        :code="DROPDOWN_CODE"
        :collapsible="true"
        :default-open="false"
        filename="DropdownMenu.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="sheet">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Sheet</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          A slide-in panel from any screen edge with focus trap and ARIA dialog semantics.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <Sheet :open="sheetOpen" @openChange="val => (sheetOpen = val)">
          <SheetTrigger as-child>
            <Button variant="outline" @click="sheetOpen = true">Open Sheet</Button>
          </SheetTrigger>
          <SheetContent side="right">
            <SheetHeader>
              <SheetTitle>Edit profile</SheetTitle>
              <SheetDescription>
                Make changes to your profile here. Click save when you're done.
              </SheetDescription>
            </SheetHeader>
            <div class="py-6">
              <p class="text-sm text-muted-foreground">Sheet content goes here.</p>
            </div>
            <SheetFooter>
              <SheetClose as-child>
                <Button variant="outline" @click="sheetOpen = false">Close</Button>
              </SheetClose>
            </SheetFooter>
          </SheetContent>
        </Sheet>
      </div>
      <CodeBlock
        :code="SHEET_CODE"
        :collapsible="true"
        :default-open="false"
        filename="Sheet.vue"
        language="vue"
      />
    </section>
  </div>
</template>
