<script lang="ts">
  import { Button } from "$lib/components/ui/button";
  import { CodeBlock } from "$lib/components/ui/code-block";
  import {
    DropdownMenu,
    DropdownMenuContent,
    DropdownMenuItem,
    DropdownMenuLabel,
    DropdownMenuSeparator,
    DropdownMenuTrigger,
  } from "$lib/components/ui/dropdown-menu";
  import { Popover, PopoverContent, PopoverTrigger } from "$lib/components/ui/popover";
  import {
    Sheet,
    SheetClose,
    SheetContent,
    SheetDescription,
    SheetFooter,
    SheetHeader,
    SheetTitle,
    SheetTrigger,
  } from "$lib/components/ui/sheet";

  const DROPDOWN_MENU_CODE = `<script lang="ts">
  import { Button } from "$lib/components/ui/button";
  import {
    DropdownMenu,
    DropdownMenuContent,
    DropdownMenuItem,
    DropdownMenuLabel,
    DropdownMenuSeparator,
    DropdownMenuTrigger,
  } from "$lib/components/ui/dropdown-menu";

  let open = $state(false);
<\/script>

<DropdownMenu onOpenChange={(val) => (open = val)} {open}>
  <DropdownMenuTrigger>
    <Button variant="outline">Options</Button>
  </DropdownMenuTrigger>
  <DropdownMenuContent>
    <DropdownMenuLabel>My Account</DropdownMenuLabel>
    <DropdownMenuSeparator />
    <DropdownMenuItem>Profile</DropdownMenuItem>
    <DropdownMenuItem>Settings</DropdownMenuItem>
    <DropdownMenuItem>Sign out</DropdownMenuItem>
  </DropdownMenuContent>
</DropdownMenu>`;

  const POPOVER_CODE = `<script lang="ts">
  import { Button } from "$lib/components/ui/button";
  import { Popover, PopoverContent, PopoverTrigger } from "$lib/components/ui/popover";

  let open = $state(false);
<\/script>

<Popover onOpenChange={(val) => (open = val)} {open}>
  <PopoverTrigger>
    <Button variant="outline">Open Popover</Button>
  </PopoverTrigger>
  <PopoverContent>
    <p class="text-sm">This is a popover with some helpful content.</p>
    <Button onclick={() => (open = false)} size="sm" variant="ghost">Close</Button>
  </PopoverContent>
</Popover>`;

  const SHEET_CODE = `<script lang="ts">
  import { Button } from "$lib/components/ui/button";
  import {
    Sheet,
    SheetClose,
    SheetContent,
    SheetDescription,
    SheetFooter,
    SheetHeader,
    SheetTitle,
    SheetTrigger,
  } from "$lib/components/ui/sheet";

  let open = $state(false);
<\/script>

<Sheet onOpenChange={(val) => (open = val)} {open}>
  <SheetTrigger>
    <Button variant="outline">Open Sheet</Button>
  </SheetTrigger>
  <SheetContent side="right">
    <SheetHeader>
      <SheetTitle>Edit Profile</SheetTitle>
      <SheetDescription>
        Make changes to your profile here. Click close when you are done.
      </SheetDescription>
    </SheetHeader>
    <SheetFooter>
      <SheetClose>
        <Button variant="outline">Close</Button>
      </SheetClose>
    </SheetFooter>
  </SheetContent>
</Sheet>`;

  let dropdownOpen = $state(false);
  let popoverOpen = $state(false);
  let sheetOpen = $state(false);
</script>

<div class="flex flex-col gap-16">
  <div>
    <h1 class="text-3xl font-bold tracking-tight text-foreground">Overlay</h1>
    <p class="mt-2 text-muted-foreground">
      Floating panels, dropdown menus, and slide-over sheets for contextual
      interactions.
    </p>
  </div>

  <section class="flex flex-col gap-4" id="popover">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">Popover</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A floating panel anchored to a trigger element for contextual information
        or inline actions.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <Popover onOpenChange={(val) => (popoverOpen = val)} open={popoverOpen}>
        <PopoverTrigger>
          <Button variant="outline">Open Popover</Button>
        </PopoverTrigger>
        <PopoverContent>
          <p class="text-sm">This is a popover with some helpful content.</p>
          <Button
            onclick={() => (popoverOpen = false)}
            size="sm"
            variant="ghost"
          >
            Close
          </Button>
        </PopoverContent>
      </Popover>
    </div>
    <CodeBlock
      code={POPOVER_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="Popover.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="dropdown-menu">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">DropdownMenu</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A menu of actions that appears below a trigger with full keyboard and ARIA
        support.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <DropdownMenu
        onOpenChange={(val) => (dropdownOpen = val)}
        open={dropdownOpen}
      >
        <DropdownMenuTrigger>
          <Button variant="outline">Options</Button>
        </DropdownMenuTrigger>
        <DropdownMenuContent>
          <DropdownMenuLabel>My Account</DropdownMenuLabel>
          <DropdownMenuSeparator />
          <DropdownMenuItem>Profile</DropdownMenuItem>
          <DropdownMenuItem>Settings</DropdownMenuItem>
          <DropdownMenuItem>Sign out</DropdownMenuItem>
        </DropdownMenuContent>
      </DropdownMenu>
    </div>
    <CodeBlock
      code={DROPDOWN_MENU_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="DropdownMenu.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="sheet">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">Sheet</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A slide-over panel that animates in from the edge of the screen for
        secondary content.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <Sheet onOpenChange={(val) => (sheetOpen = val)} open={sheetOpen}>
        <SheetTrigger>
          <Button variant="outline">Open Sheet</Button>
        </SheetTrigger>
        <SheetContent side="right">
          <SheetHeader>
            <SheetTitle>Edit Profile</SheetTitle>
            <SheetDescription>
              Make changes to your profile here. Click close when you are done.
            </SheetDescription>
          </SheetHeader>
          <SheetFooter>
            <SheetClose>
              <Button variant="outline">Close</Button>
            </SheetClose>
          </SheetFooter>
        </SheetContent>
      </Sheet>
    </div>
    <CodeBlock
      code={SHEET_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="Sheet.svelte"
      language="svelte"
    />
  </section>
</div>
