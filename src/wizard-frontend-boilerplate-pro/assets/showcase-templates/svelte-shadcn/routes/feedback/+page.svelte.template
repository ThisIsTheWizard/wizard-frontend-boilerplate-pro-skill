<script lang="ts">
  import { toast, Toaster } from "svelte-sonner";

  import { Alert, AlertDescription, AlertTitle } from "$lib/components/ui/alert";
  import { Button } from "$lib/components/ui/button";
  import { CodeBlock } from "$lib/components/ui/code-block";
  import {
    Dialog,
    DialogClose,
    DialogContent,
    DialogDescription,
    DialogFooter,
    DialogHeader,
    DialogTitle,
    DialogTrigger,
  } from "$lib/components/ui/dialog";
  import { Progress } from "$lib/components/ui/progress";
  import {
    Tooltip,
    TooltipContent,
    TooltipProvider,
    TooltipTrigger,
  } from "$lib/components/ui/tooltip";

  let dialogOpen = $state(false);

  const ALERT_CODE = `<script lang="ts">
  import { Alert, AlertDescription, AlertTitle } from "$lib/components/ui/alert";
<\/script>

<div class="flex flex-col gap-3">
  <Alert>
    <AlertTitle>Heads up</AlertTitle>
    <AlertDescription>
      You can add components to your app using the CLI.
    </AlertDescription>
  </Alert>
  <Alert variant="destructive">
    <AlertTitle>Error</AlertTitle>
    <AlertDescription>Something went wrong. Please try again.</AlertDescription>
  </Alert>
</div>`;

  const TOAST_CODE = `<script lang="ts">
  import { toast, Toaster } from "svelte-sonner";
  import { Button } from "$lib/components/ui/button";
<\/script>

<!-- Place <Toaster /> once in your root layout. -->

<Button onclick={() => toast("Event has been created.")}>Default</Button>
<Button variant="outline" onclick={() => toast.success("Profile saved.")}>
  Success
</Button>`;

  const PROGRESS_CODE = `<script lang="ts">
  import { Progress } from "$lib/components/ui/progress";
<\/script>

<div class="flex flex-col gap-4">
  <Progress value={25} />
  <Progress value={60} />
  <Progress value={100} />
</div>`;

  const TOOLTIP_CODE = `<script lang="ts">
  import { Button } from "$lib/components/ui/button";
  import {
    Tooltip, TooltipContent, TooltipProvider, TooltipTrigger,
  } from "$lib/components/ui/tooltip";
<\/script>

<!-- TooltipProvider is typically placed once in root layout. -->

<TooltipProvider>
  <Tooltip>
    <TooltipTrigger asChild>
      <Button variant="outline">Hover me</Button>
    </TooltipTrigger>
    <TooltipContent>Tooltip content</TooltipContent>
  </Tooltip>
</TooltipProvider>`;

  const DIALOG_CODE = `<script lang="ts">
  import { Button } from "$lib/components/ui/button";
  import {
    Dialog, DialogClose, DialogContent, DialogDescription,
    DialogFooter, DialogHeader, DialogTitle, DialogTrigger,
  } from "$lib/components/ui/dialog";

  let open = $state(false);
<\/script>

<Dialog bind:open>
  <DialogTrigger asChild>
    <Button>Open dialog</Button>
  </DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Confirm action</DialogTitle>
      <DialogDescription>
        This action cannot be undone. Are you sure you want to proceed?
      </DialogDescription>
    </DialogHeader>
    <DialogFooter>
      <DialogClose asChild>
        <Button variant="outline">Cancel</Button>
      </DialogClose>
      <Button onclick={() => (open = false)}>Confirm</Button>
    </DialogFooter>
  </DialogContent>
</Dialog>`;
</script>

<div class="flex flex-col gap-16">
  <div>
    <h1 class="text-3xl font-bold tracking-tight text-foreground">Feedback</h1>
    <p class="mt-2 text-muted-foreground">
      Status indicators, notifications, and modal interactions — alerts,
      toasts, dialogs, and progress.
    </p>
  </div>

  <section class="flex flex-col gap-4" id="alert">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">Alert</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        An inline message banner. shadcn-svelte ships
        <code class="rounded bg-muted px-1 py-0.5 text-xs">default</code> and
        <code class="rounded bg-muted px-1 py-0.5 text-xs">destructive</code> variants —
        add success/warning via CVA if needed.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <div class="flex flex-col gap-3">
        <Alert>
          <AlertTitle>Heads up</AlertTitle>
          <AlertDescription>
            You can add components to your app using the CLI.
          </AlertDescription>
        </Alert>
        <Alert variant="destructive">
          <AlertTitle>Error</AlertTitle>
          <AlertDescription>
            Something went wrong. Please try again.
          </AlertDescription>
        </Alert>
      </div>
    </div>
    <CodeBlock
      code={ALERT_CODE}
      collapsible
      defaultOpen={false}
      filename="Alert.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="toast">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">Toast</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        Transient notification messages powered by svelte-sonner. Place
        <code class="rounded bg-muted px-1 py-0.5 text-xs">&lt;Toaster /&gt;</code>
        once in your root layout.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <Toaster />
      <div class="flex flex-wrap gap-2">
        <Button onclick={() => toast("Event has been created.")}>Default</Button>
        <Button
          onclick={() => toast.success("Profile saved successfully.")}
          variant="outline"
        >
          Success
        </Button>
        <Button
          onclick={() => toast.error("Something went wrong.")}
          variant="outline"
        >
          Error
        </Button>
        <Button
          onclick={() => toast.warning("Unsaved changes detected.")}
          variant="outline"
        >
          Warning
        </Button>
      </div>
    </div>
    <CodeBlock
      code={TOAST_CODE}
      collapsible
      defaultOpen={false}
      filename="Toast.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="progress">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">Progress</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A horizontal bar showing completion status. Pass
        <code class="rounded bg-muted px-1 py-0.5 text-xs">value</code> (0–100).
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <div class="flex flex-col gap-4">
        <Progress value={25} />
        <Progress value={60} />
        <Progress value={100} />
      </div>
    </div>
    <CodeBlock
      code={PROGRESS_CODE}
      collapsible
      defaultOpen={false}
      filename="Progress.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="tooltip">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">Tooltip</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A floating label on hover/focus. Requires
        <code class="rounded bg-muted px-1 py-0.5 text-xs">TooltipProvider</code>
        wrapping the tooltip tree (typically in root layout).
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <TooltipProvider>
        <div class="flex flex-wrap items-center gap-4">
          <Tooltip>
            <TooltipTrigger asChild>
              <Button variant="outline">Top</Button>
            </TooltipTrigger>
            <TooltipContent side="top">Tooltip on top</TooltipContent>
          </Tooltip>
          <Tooltip>
            <TooltipTrigger asChild>
              <Button variant="outline">Right</Button>
            </TooltipTrigger>
            <TooltipContent side="right">Tooltip on right</TooltipContent>
          </Tooltip>
          <Tooltip>
            <TooltipTrigger asChild>
              <Button variant="outline">Bottom</Button>
            </TooltipTrigger>
            <TooltipContent side="bottom">Tooltip on bottom</TooltipContent>
          </Tooltip>
          <Tooltip>
            <TooltipTrigger asChild>
              <Button variant="outline">Left</Button>
            </TooltipTrigger>
            <TooltipContent side="left">Tooltip on left</TooltipContent>
          </Tooltip>
        </div>
      </TooltipProvider>
    </div>
    <CodeBlock
      code={TOOLTIP_CODE}
      collapsible
      defaultOpen={false}
      filename="Tooltip.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="dialog">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">Dialog</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A modal overlay with a trigger, header, body, and footer actions —
        focus-trapped and accessible.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <Dialog bind:open={dialogOpen}>
        <DialogTrigger asChild>
          <Button>Open dialog</Button>
        </DialogTrigger>
        <DialogContent>
          <DialogHeader>
            <DialogTitle>Confirm action</DialogTitle>
            <DialogDescription>
              This action cannot be undone. Are you sure you want to proceed?
            </DialogDescription>
          </DialogHeader>
          <DialogFooter>
            <DialogClose asChild>
              <Button variant="outline">Cancel</Button>
            </DialogClose>
            <Button onclick={() => (dialogOpen = false)}>Confirm</Button>
          </DialogFooter>
        </DialogContent>
      </Dialog>
    </div>
    <CodeBlock
      code={DIALOG_CODE}
      collapsible
      defaultOpen={false}
      filename="Dialog.svelte"
      language="svelte"
    />
  </section>
</div>
