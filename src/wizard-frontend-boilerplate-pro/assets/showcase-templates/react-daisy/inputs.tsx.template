"use client";

import { useState } from "react";

import { CodeBlock } from "@/components/ui/code-block";

const BUTTON_CODE = `export function Example() {
  return (
    <div className="flex flex-col gap-2">
      <div className="flex flex-wrap gap-2">
        <button className="btn btn-primary" type="button">Primary</button>
        <button className="btn btn-outline btn-primary" type="button">Outline</button>
        <button className="btn btn-secondary" type="button">Secondary</button>
        <button className="btn btn-error" type="button">Error</button>
        <button className="btn btn-ghost" type="button">Ghost</button>
      </div>
      <div className="flex flex-wrap items-center gap-2">
        <button className="btn btn-primary btn-sm" type="button">Small</button>
        <button className="btn btn-primary" type="button">Default</button>
        <button className="btn btn-primary btn-lg" type="button">Large</button>
        <button className="btn btn-primary btn-disabled" disabled type="button">Disabled</button>
      </div>
    </div>
  );
}`;

const INPUT_CODE = `export function Example() {
  return (
    <div className="flex flex-col gap-3" style={{ maxWidth: 360 }}>
      <label className="form-control w-full">
        <div className="label">
          <span className="label-text">Email address</span>
        </div>
        <input
          className="input input-bordered w-full"
          placeholder="name@example.com"
          type="email"
        />
        <div className="label">
          <span className="label-text-alt text-base-content/60">
            We'll never share your email.
          </span>
        </div>
      </label>

      <label className="form-control w-full">
        <div className="label">
          <span className="label-text">Username</span>
        </div>
        <input
          className="input input-bordered input-error w-full"
          defaultValue="invalid user!"
          type="text"
        />
        <div className="label">
          <span className="label-text-alt text-error">
            Please enter a valid username.
          </span>
        </div>
      </label>

      <label className="form-control w-full">
        <div className="label">
          <span className="label-text">Disabled</span>
        </div>
        <input
          className="input input-bordered w-full"
          disabled
          placeholder="Not editable"
          type="text"
        />
      </label>
    </div>
  );
}`;

const TEXTAREA_CODE = `export function Example() {
  return (
    <div className="flex flex-col gap-3" style={{ maxWidth: 360 }}>
      <label className="form-control w-full">
        <div className="label">
          <span className="label-text">Bio</span>
        </div>
        <textarea
          className="textarea textarea-bordered w-full"
          placeholder="Tell us about yourself…"
          rows={4}
        />
        <div className="label">
          <span className="label-text-alt text-base-content/60">
            Describe your project in a few sentences.
          </span>
        </div>
      </label>

      <label className="form-control w-full">
        <div className="label">
          <span className="label-text">Message</span>
        </div>
        <textarea
          className="textarea textarea-bordered textarea-error w-full"
          placeholder="Enter your message…"
          rows={3}
        />
        <div className="label">
          <span className="label-text-alt text-error">Message is required.</span>
        </div>
      </label>
    </div>
  );
}`;

const SELECT_CODE = `export function Example() {
  return (
    <div className="flex flex-col gap-3" style={{ maxWidth: 360 }}>
      <label className="form-control w-full">
        <div className="label">
          <span className="label-text">Framework</span>
        </div>
        <select className="select select-bordered w-full">
          <option disabled value="">Select a framework…</option>
          <option value="nextjs">Next.js</option>
          <option value="remix">Remix</option>
          <option value="astro">Astro</option>
          <option value="sveltekit">SvelteKit</option>
        </select>
      </label>

      <label className="form-control w-full">
        <div className="label">
          <span className="label-text">Disabled</span>
        </div>
        <select className="select select-bordered w-full" disabled>
          <option value="">None</option>
        </select>
      </label>
    </div>
  );
}`;

const CHECKBOX_CODE = `export function Example() {
  return (
    <div className="flex flex-col gap-3">
      <label className="flex items-center gap-3 cursor-pointer">
        <input className="checkbox checkbox-primary" type="checkbox" />
        <span className="label-text">Accept terms and conditions</span>
      </label>
      <label className="flex items-center gap-3 cursor-pointer">
        <input className="checkbox checkbox-primary" defaultChecked type="checkbox" />
        <span className="label-text">Checked by default</span>
      </label>
      <label className="flex items-center gap-3 cursor-pointer opacity-50">
        <input className="checkbox" disabled type="checkbox" />
        <span className="label-text">Disabled</span>
      </label>
    </div>
  );
}`;

const RADIO_CODE = `import { useState } from "react";

export function Example() {
  const [plan, setPlan] = useState("free");

  return (
    <div className="flex flex-col gap-2">
      <span className="label-text font-medium">Plan</span>
      {[
        { label: "Free", value: "free" },
        { label: "Pro", value: "pro" },
        { label: "Enterprise", value: "enterprise" },
      ].map(({ label, value }) => (
        <label className="flex items-center gap-3 cursor-pointer" key={value}>
          <input
            checked={plan === value}
            className="radio radio-primary"
            name="plan"
            onChange={() => setPlan(value)}
            type="radio"
            value={value}
          />
          <span className="label-text">{label}</span>
        </label>
      ))}
    </div>
  );
}`;

const TOGGLE_CODE = `export function Example() {
  return (
    <div className="flex flex-col gap-3">
      <label className="flex items-center gap-3 cursor-pointer">
        <input className="toggle toggle-primary" type="checkbox" />
        <span className="label-text">Email notifications</span>
      </label>
      <label className="flex items-center gap-3 cursor-pointer">
        <input className="toggle toggle-primary" defaultChecked type="checkbox" />
        <span className="label-text">Marketing emails</span>
      </label>
      <label className="flex items-center gap-3 cursor-pointer opacity-50">
        <input className="toggle" disabled type="checkbox" />
        <span className="label-text">Disabled</span>
      </label>
    </div>
  );
}`;

export default function InputsPage() {
  const [plan, setPlan] = useState("free");

  return (
    <div className="flex flex-col gap-4">
      <div>
        <h1 className="text-xl font-semibold mb-1">Inputs</h1>
        <p className="text-base-content/60 text-sm">
          Form controls for collecting user input — buttons, text fields,
          selections, and toggles.
        </p>
      </div>

      <section className="mb-2" id="button">
        <div className="mb-2 pl-3" style={{ borderLeft: "3px solid var(--color-primary)" }}>
          <h5 className="font-semibold mb-1">Button</h5>
          <p className="text-base-content/60 text-sm mb-0">
            DaisyUI ships <code className="kbd kbd-sm">btn</code> with semantic
            colour modifiers (<code className="kbd kbd-sm">btn-primary</code>,{" "}
            <code className="kbd kbd-sm">btn-secondary</code>,{" "}
            <code className="kbd kbd-sm">btn-error</code>,{" "}
            <code className="kbd kbd-sm">btn-ghost</code>,{" "}
            <code className="kbd kbd-sm">btn-outline</code>), size modifiers (
            <code className="kbd kbd-sm">btn-sm</code>,{" "}
            <code className="kbd kbd-sm">btn-lg</code>), and a{" "}
            <code className="kbd kbd-sm">btn-disabled</code> state.
          </p>
        </div>
        <div className="bg-base-200 rounded-box p-4 mb-3">
          <div className="flex flex-col gap-2">
            <div className="flex flex-wrap gap-2">
              <button className="btn btn-primary" type="button">Primary</button>
              <button className="btn btn-outline btn-primary" type="button">Outline</button>
              <button className="btn btn-secondary" type="button">Secondary</button>
              <button className="btn btn-error" type="button">Error</button>
              <button className="btn btn-ghost" type="button">Ghost</button>
            </div>
            <div className="flex flex-wrap items-center gap-2">
              <button className="btn btn-primary btn-sm" type="button">Small</button>
              <button className="btn btn-primary" type="button">Default</button>
              <button className="btn btn-primary btn-lg" type="button">Large</button>
              <button className="btn btn-primary btn-disabled" disabled type="button">Disabled</button>
            </div>
          </div>
        </div>
        <CodeBlock
          code={BUTTON_CODE}
          collapsible
          defaultOpen={false}
          filename="Button.tsx"
          language="tsx"
        />
      </section>

      <div className="divider my-2" />

      <section className="mb-2" id="input">
        <div className="mb-2 pl-3" style={{ borderLeft: "3px solid var(--color-primary)" }}>
          <h5 className="font-semibold mb-1">Input</h5>
          <p className="text-base-content/60 text-sm mb-0">
            A text field using{" "}
            <code className="kbd kbd-sm">input input-bordered</code>. Wrap in{" "}
            <code className="kbd kbd-sm">{"<label class=\"form-control\">"}</code>{" "}
            to get DaisyUI label and helper text slots. Add{" "}
            <code className="kbd kbd-sm">input-error</code> for validation state.
          </p>
        </div>
        <div className="bg-base-200 rounded-box p-4 mb-3">
          <div className="flex flex-col gap-3" style={{ maxWidth: 360 }}>
            <label className="form-control w-full">
              <div className="label">
                <span className="label-text">Email address</span>
              </div>
              <input
                className="input input-bordered w-full"
                placeholder="name@example.com"
                type="email"
              />
              <div className="label">
                <span className="label-text-alt text-base-content/60">
                  We&apos;ll never share your email.
                </span>
              </div>
            </label>

            <label className="form-control w-full">
              <div className="label">
                <span className="label-text">Username</span>
              </div>
              <input
                className="input input-bordered input-error w-full"
                defaultValue="invalid user!"
                type="text"
              />
              <div className="label">
                <span className="label-text-alt text-error">
                  Please enter a valid username.
                </span>
              </div>
            </label>

            <label className="form-control w-full">
              <div className="label">
                <span className="label-text">Disabled</span>
              </div>
              <input
                className="input input-bordered w-full"
                disabled
                placeholder="Not editable"
                type="text"
              />
            </label>
          </div>
        </div>
        <CodeBlock
          code={INPUT_CODE}
          collapsible
          defaultOpen={false}
          filename="Input.tsx"
          language="tsx"
        />
      </section>

      <div className="divider my-2" />

      <section className="mb-2" id="textarea">
        <div className="mb-2 pl-3" style={{ borderLeft: "3px solid var(--color-primary)" }}>
          <h5 className="font-semibold mb-1">Textarea</h5>
          <p className="text-base-content/60 text-sm mb-0">
            A multi-line input using{" "}
            <code className="kbd kbd-sm">textarea textarea-bordered</code>. Add{" "}
            <code className="kbd kbd-sm">textarea-error</code> for validation
            state.
          </p>
        </div>
        <div className="bg-base-200 rounded-box p-4 mb-3">
          <div className="flex flex-col gap-3" style={{ maxWidth: 360 }}>
            <label className="form-control w-full">
              <div className="label">
                <span className="label-text">Bio</span>
              </div>
              <textarea
                className="textarea textarea-bordered w-full"
                placeholder="Tell us about yourself…"
                rows={4}
              />
              <div className="label">
                <span className="label-text-alt text-base-content/60">
                  Describe your project in a few sentences.
                </span>
              </div>
            </label>

            <label className="form-control w-full">
              <div className="label">
                <span className="label-text">Message</span>
              </div>
              <textarea
                className="textarea textarea-bordered textarea-error w-full"
                placeholder="Enter your message…"
                rows={3}
              />
              <div className="label">
                <span className="label-text-alt text-error">Message is required.</span>
              </div>
            </label>
          </div>
        </div>
        <CodeBlock
          code={TEXTAREA_CODE}
          collapsible
          defaultOpen={false}
          filename="Textarea.tsx"
          language="tsx"
        />
      </section>

      <div className="divider my-2" />

      <section className="mb-2" id="select">
        <div className="mb-2 pl-3" style={{ borderLeft: "3px solid var(--color-primary)" }}>
          <h5 className="font-semibold mb-1">Select</h5>
          <p className="text-base-content/60 text-sm mb-0">
            A native <code className="kbd kbd-sm">{"<select>"}</code> styled
            with <code className="kbd kbd-sm">select select-bordered</code>.
            Pair with <code className="kbd kbd-sm">form-control</code> and a{" "}
            <code className="kbd kbd-sm">label</code> for accessibility.
          </p>
        </div>
        <div className="bg-base-200 rounded-box p-4 mb-3">
          <div className="flex flex-col gap-3" style={{ maxWidth: 360 }}>
            <label className="form-control w-full">
              <div className="label">
                <span className="label-text">Framework</span>
              </div>
              <select className="select select-bordered w-full">
                <option disabled value="">Select a framework…</option>
                <option value="nextjs">Next.js</option>
                <option value="remix">Remix</option>
                <option value="astro">Astro</option>
                <option value="sveltekit">SvelteKit</option>
              </select>
            </label>

            <label className="form-control w-full">
              <div className="label">
                <span className="label-text">Disabled</span>
              </div>
              <select className="select select-bordered w-full" disabled>
                <option value="">None</option>
              </select>
            </label>
          </div>
        </div>
        <CodeBlock
          code={SELECT_CODE}
          collapsible
          defaultOpen={false}
          filename="Select.tsx"
          language="tsx"
        />
      </section>

      <div className="divider my-2" />

      <section className="mb-2" id="checkbox">
        <div className="mb-2 pl-3" style={{ borderLeft: "3px solid var(--color-primary)" }}>
          <h5 className="font-semibold mb-1">Checkbox</h5>
          <p className="text-base-content/60 text-sm mb-0">
            A binary toggle using{" "}
            <code className="kbd kbd-sm">checkbox checkbox-primary</code>.
            Wrap in a <code className="kbd kbd-sm">{"<label>"}</code> with{" "}
            <code className="kbd kbd-sm">cursor-pointer</code> so the entire
            row is clickable.
          </p>
        </div>
        <div className="bg-base-200 rounded-box p-4 mb-3">
          <div className="flex flex-col gap-3">
            <label className="flex items-center gap-3 cursor-pointer">
              <input className="checkbox checkbox-primary" type="checkbox" />
              <span className="label-text">Accept terms and conditions</span>
            </label>
            <label className="flex items-center gap-3 cursor-pointer">
              <input className="checkbox checkbox-primary" defaultChecked type="checkbox" />
              <span className="label-text">Checked by default</span>
            </label>
            <label className="flex items-center gap-3 cursor-pointer opacity-50">
              <input className="checkbox" disabled type="checkbox" />
              <span className="label-text">Disabled</span>
            </label>
          </div>
        </div>
        <CodeBlock
          code={CHECKBOX_CODE}
          collapsible
          defaultOpen={false}
          filename="Checkbox.tsx"
          language="tsx"
        />
      </section>

      <div className="divider my-2" />

      <section className="mb-2" id="radio-group">
        <div className="mb-2 pl-3" style={{ borderLeft: "3px solid var(--color-primary)" }}>
          <h5 className="font-semibold mb-1">RadioGroup</h5>
          <p className="text-base-content/60 text-sm mb-0">
            Mutually exclusive options using{" "}
            <code className="kbd kbd-sm">radio radio-primary</code> with a
            shared <code className="kbd kbd-sm">name</code> attribute.
          </p>
        </div>
        <div className="bg-base-200 rounded-box p-4 mb-3">
          <div className="flex flex-col gap-2">
            <span className="label-text font-medium">Plan</span>
            {(["free", "pro", "enterprise"] as const).map((value) => (
              <label className="flex items-center gap-3 cursor-pointer" key={value}>
                <input
                  checked={plan === value}
                  className="radio radio-primary"
                  name="plan-demo"
                  onChange={() => setPlan(value)}
                  type="radio"
                  value={value}
                />
                <span className="label-text capitalize">{value}</span>
              </label>
            ))}
          </div>
        </div>
        <CodeBlock
          code={RADIO_CODE}
          collapsible
          defaultOpen={false}
          filename="RadioGroup.tsx"
          language="tsx"
        />
      </section>

      <div className="divider my-2" />

      <section className="mb-2" id="toggle">
        <div className="mb-2 pl-3" style={{ borderLeft: "3px solid var(--color-primary)" }}>
          <h5 className="font-semibold mb-1">Toggle</h5>
          <p className="text-base-content/60 text-sm mb-0">
            An on/off slide control using{" "}
            <code className="kbd kbd-sm">toggle toggle-primary</code> on a
            checkbox input — renders as a pill-shaped toggle.
          </p>
        </div>
        <div className="bg-base-200 rounded-box p-4 mb-3">
          <div className="flex flex-col gap-3">
            <label className="flex items-center gap-3 cursor-pointer">
              <input className="toggle toggle-primary" type="checkbox" />
              <span className="label-text">Email notifications</span>
            </label>
            <label className="flex items-center gap-3 cursor-pointer">
              <input className="toggle toggle-primary" defaultChecked type="checkbox" />
              <span className="label-text">Marketing emails</span>
            </label>
            <label className="flex items-center gap-3 cursor-pointer opacity-50">
              <input className="toggle" disabled type="checkbox" />
              <span className="label-text">Disabled</span>
            </label>
          </div>
        </div>
        <CodeBlock
          code={TOGGLE_CODE}
          collapsible
          defaultOpen={false}
          filename="Toggle.tsx"
          language="tsx"
        />
      </section>
    </div>
  );
}
