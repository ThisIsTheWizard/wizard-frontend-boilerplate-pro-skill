<script lang="ts">
  import { Avatar } from "$lib/components/ui/avatar";
  import { Badge } from "$lib/components/ui/badge";
  import { Button } from "$lib/components/ui/button";
  import {
    Card,
    CardContent,
    CardDescription,
    CardFooter,
    CardHeader,
    CardTitle,
  } from "$lib/components/ui/card";
  import { CodeBlock } from "$lib/components/ui/code-block";
  import { Separator } from "$lib/components/ui/separator";
  import { Skeleton } from "$lib/components/ui/skeleton";
  import {
    Table,
    TableBody,
    TableCaption,
    TableCell,
    TableHead,
    TableHeader,
    TableRow,
  } from "$lib/components/ui/table";

  const AVATAR_CODE = `<script lang="ts">
  import { Avatar } from "$lib/components/ui/avatar";
<\/script>

<div class="flex items-center gap-4">
  <Avatar alt="Jane Doe" src="https://i.pravatar.cc/150?img=1" />
  <Avatar fallback="JD" />
  <Avatar fallback="SM" size="sm" />
  <Avatar fallback="LG" size="lg" />
</div>`;

  const BADGE_CODE = `<script lang="ts">
  import { Badge } from "$lib/components/ui/badge";
<\/script>

<div class="flex flex-wrap gap-2">
  <Badge>Default</Badge>
  <Badge variant="secondary">Secondary</Badge>
  <Badge variant="destructive">Destructive</Badge>
  <Badge variant="outline">Outline</Badge>
</div>`;

  const CARD_CODE = `<script lang="ts">
  import { Button } from "$lib/components/ui/button";
  import {
    Card,
    CardContent,
    CardDescription,
    CardFooter,
    CardHeader,
    CardTitle,
  } from "$lib/components/ui/card";
<\/script>

<Card class="w-80">
  <CardHeader>
    <CardTitle>Project Alpha</CardTitle>
    <CardDescription>A next-generation web application.</CardDescription>
  </CardHeader>
  <CardContent>
    <p class="text-sm text-muted-foreground">
      This project aims to redefine how teams collaborate in real time.
    </p>
  </CardContent>
  <CardFooter>
    <Button variant="outline">View details</Button>
  </CardFooter>
</Card>`;

  const SEPARATOR_CODE = `<script lang="ts">
  import { Separator } from "$lib/components/ui/separator";
<\/script>

<div class="flex flex-col gap-4">
  <div class="flex flex-col gap-2">
    <p class="text-sm text-muted-foreground">Section one content.</p>
    <Separator />
    <p class="text-sm text-muted-foreground">Section two content.</p>
  </div>
  <div class="flex items-center gap-4">
    <span class="text-sm">Left item</span>
    <Separator class="h-4" orientation="vertical" />
    <span class="text-sm">Right item</span>
  </div>
</div>`;

  const SKELETON_CODE = `<script lang="ts">
  import { Skeleton } from "$lib/components/ui/skeleton";
<\/script>

<div class="flex items-center gap-4">
  <Skeleton class="h-12 w-12 rounded-full" />
  <div class="flex flex-col gap-2">
    <Skeleton class="h-4 w-48" />
    <Skeleton class="h-4 w-36" />
    <Skeleton class="h-4 w-24" />
  </div>
</div>`;

  const TABLE_CODE = `<script lang="ts">
  import {
    Table,
    TableBody,
    TableCaption,
    TableCell,
    TableHead,
    TableHeader,
    TableRow,
  } from "$lib/components/ui/table";
<\/script>

<Table>
  <TableCaption>Team members list</TableCaption>
  <TableHeader>
    <TableRow>
      <TableHead scope="col">Name</TableHead>
      <TableHead scope="col">Email</TableHead>
      <TableHead scope="col">Role</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell>Alice Martin</TableCell>
      <TableCell>alice@example.com</TableCell>
      <TableCell>Admin</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Bob Chen</TableCell>
      <TableCell>bob@example.com</TableCell>
      <TableCell>Editor</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Carol White</TableCell>
      <TableCell>carol@example.com</TableCell>
      <TableCell>Viewer</TableCell>
    </TableRow>
  </TableBody>
</Table>`;
</script>

<div class="flex flex-col gap-16">
  <div>
    <h1 class="text-3xl font-bold tracking-tight text-foreground">Display</h1>
    <p class="mt-2 text-muted-foreground">
      Read-only visual elements for presenting content — cards, badges, avatars,
      and data tables.
    </p>
  </div>

  <section class="flex flex-col gap-4" id="card">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">Card</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A surface for grouping related content with an optional header, body, and
        footer.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <Card class="w-80">
        <CardHeader>
          <CardTitle>Project Alpha</CardTitle>
          <CardDescription>A next-generation web application.</CardDescription>
        </CardHeader>
        <CardContent>
          <p class="text-sm text-muted-foreground">
            This project aims to redefine how teams collaborate in real time.
          </p>
        </CardContent>
        <CardFooter>
          <Button variant="outline">View details</Button>
        </CardFooter>
      </Card>
    </div>
    <CodeBlock
      code={CARD_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="Card.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="badge">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">Badge</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A compact label for status, category, or count with four visual variants.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <div class="flex flex-wrap gap-2">
        <Badge>Default</Badge>
        <Badge variant="secondary">Secondary</Badge>
        <Badge variant="destructive">Destructive</Badge>
        <Badge variant="outline">Outline</Badge>
      </div>
    </div>
    <CodeBlock
      code={BADGE_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="Badge.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="avatar">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">Avatar</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A circular user image that falls back to initials when no src is available,
        with small, default, and large sizes.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <div class="flex items-center gap-4">
        <Avatar alt="Jane Doe" src="https://i.pravatar.cc/150?img=1" />
        <Avatar fallback="JD" />
        <Avatar fallback="SM" size="sm" />
        <Avatar fallback="LG" size="lg" />
      </div>
    </div>
    <CodeBlock
      code={AVATAR_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="Avatar.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="separator">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">Separator</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A thin dividing line for separating sections, available in horizontal and
        vertical orientations.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <div class="flex flex-col gap-6">
        <div class="flex flex-col gap-2">
          <p class="text-sm text-muted-foreground">Section one content.</p>
          <Separator />
          <p class="text-sm text-muted-foreground">Section two content.</p>
        </div>
        <div class="flex items-center gap-4">
          <span class="text-sm">Left item</span>
          <Separator class="h-4" orientation="vertical" />
          <span class="text-sm">Right item</span>
        </div>
      </div>
    </div>
    <CodeBlock
      code={SEPARATOR_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="Separator.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="skeleton">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">Skeleton</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        Animated placeholder blocks that mimic content layout during loading states.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <div class="flex items-center gap-4">
        <Skeleton class="h-12 w-12 rounded-full" />
        <div class="flex flex-col gap-2">
          <Skeleton class="h-4 w-48" />
          <Skeleton class="h-4 w-36" />
          <Skeleton class="h-4 w-24" />
        </div>
      </div>
    </div>
    <CodeBlock
      code={SKELETON_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="Skeleton.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="table">
    <div class="border-l-2 border-primary/40 pl-3">
      <h2 class="text-xl font-semibold text-foreground">Table</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A structured grid for tabular data with header, body, and optional caption.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-muted/50 p-8">
      <Table>
        <TableCaption>Team members list</TableCaption>
        <TableHeader>
          <TableRow>
            <TableHead scope="col">Name</TableHead>
            <TableHead scope="col">Email</TableHead>
            <TableHead scope="col">Role</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          <TableRow>
            <TableCell>Alice Martin</TableCell>
            <TableCell>alice@example.com</TableCell>
            <TableCell>Admin</TableCell>
          </TableRow>
          <TableRow>
            <TableCell>Bob Chen</TableCell>
            <TableCell>bob@example.com</TableCell>
            <TableCell>Editor</TableCell>
          </TableRow>
          <TableRow>
            <TableCell>Carol White</TableCell>
            <TableCell>carol@example.com</TableCell>
            <TableCell>Viewer</TableCell>
          </TableRow>
        </TableBody>
      </Table>
    </div>
    <CodeBlock
      code={TABLE_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="Table.svelte"
      language="svelte"
    />
  </section>
</div>
