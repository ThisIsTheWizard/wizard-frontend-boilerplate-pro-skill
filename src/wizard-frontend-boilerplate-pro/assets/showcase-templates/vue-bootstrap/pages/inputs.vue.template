<script setup lang="ts">
import { ref } from "vue";

import {
  BButton,
  BFormCheckbox,
  BFormGroup,
  BFormInput,
  BFormRadioGroup,
  BFormSelect,
  BFormText,
  BFormTextarea,
} from "bootstrap-vue-next";

import { CodeBlock } from "@/components/ui/code-block";

const plan = ref("free");
const checked = ref(false);
const checkedDefault = ref(true);

const BUTTON_CODE = `<script setup lang="ts">
import { BButton } from "bootstrap-vue-next";
<\/script>

<template>
  <div class="d-flex flex-wrap gap-2">
    <BButton variant="primary">Primary</BButton>
    <BButton variant="secondary">Secondary</BButton>
    <BButton variant="outline-primary">Outline</BButton>
    <BButton variant="danger">Danger</BButton>
    <BButton variant="link">Link</BButton>
  </div>
</template>`;

const INPUT_CODE = `<script setup lang="ts">
import { BFormGroup, BFormInput, BFormText } from "bootstrap-vue-next";
<\/script>

<template>
  <BFormGroup id="email-group" label="Email address" label-for="email">
    <BFormInput id="email" placeholder="you@example.com" type="email" />
    <BFormText>We'll never share your email.</BFormText>
  </BFormGroup>
</template>`;

const TEXTAREA_CODE = `<script setup lang="ts">
import { BFormGroup, BFormTextarea, BFormText } from "bootstrap-vue-next";
<\/script>

<template>
  <BFormGroup id="bio-group" label="Bio" label-for="bio">
    <BFormTextarea id="bio" placeholder="Tell us about yourself…" :rows="4" />
    <BFormText>Describe your project in a few sentences.</BFormText>
  </BFormGroup>
</template>`;

const SELECT_CODE = `<script setup lang="ts">
import { BFormGroup, BFormSelect } from "bootstrap-vue-next";
<\/script>

<template>
  <BFormGroup id="framework-group" label="Framework" label-for="framework">
    <BFormSelect
      id="framework"
      :options="[
        { value: null, text: 'Select a framework…', disabled: true },
        { value: 'nextjs', text: 'Next.js' },
        { value: 'remix', text: 'Remix' },
        { value: 'astro', text: 'Astro' },
        { value: 'sveltekit', text: 'SvelteKit' },
      ]"
    />
  </BFormGroup>
</template>`;

const CHECKBOX_CODE = `<script setup lang="ts">
import { ref } from "vue";
import { BFormCheckbox } from "bootstrap-vue-next";

const checked = ref(false);
<\/script>

<template>
  <BFormCheckbox v-model="checked">
    Accept terms and conditions
  </BFormCheckbox>
</template>`;

const RADIO_GROUP_CODE = `<script setup lang="ts">
import { ref } from "vue";
import { BFormRadioGroup } from "bootstrap-vue-next";

const plan = ref("free");
<\/script>

<template>
  <BFormRadioGroup
    v-model="plan"
    :options="[
      { value: 'free', text: 'Free' },
      { value: 'pro', text: 'Pro' },
      { value: 'enterprise', text: 'Enterprise' },
    ]"
    stacked
  />
</template>`;

const SWITCH_CODE = `<script setup lang="ts">
import { ref } from "vue";
import { BFormCheckbox } from "bootstrap-vue-next";

const notifications = ref(false);
<\/script>

<template>
  <BFormCheckbox v-model="notifications" :switch="true">
    Email notifications
  </BFormCheckbox>
</template>`;
</script>

<template>
  <div class="d-flex flex-column gap-4">
    <div>
      <h1 class="h4 mb-1">Inputs</h1>
      <p class="text-body-secondary">
        Form controls for collecting user input — buttons, text fields, selections, and toggles.
      </p>
    </div>

    <!-- Button -->
    <section id="button">
      <div class="mb-2 ps-3" style="border-left: 3px solid var(--bs-primary)">
        <h5 class="mb-1">Button</h5>
        <p class="text-body-secondary mb-0 small">
          Triggers an action or event with support for multiple visual variants and sizes.
        </p>
      </div>
      <div class="p-4 rounded-3 border bg-body-tertiary mb-3 shadow-sm">
        <div class="d-flex flex-column gap-3">
          <div class="d-flex flex-wrap gap-2">
            <BButton variant="primary">Primary</BButton>
            <BButton variant="secondary">Secondary</BButton>
            <BButton variant="outline-primary">Outline</BButton>
            <BButton variant="danger">Danger</BButton>
            <BButton variant="link">Link</BButton>
          </div>
          <div class="d-flex flex-wrap align-items-center gap-2">
            <BButton size="sm" variant="primary">Small</BButton>
            <BButton variant="primary">Default</BButton>
            <BButton size="lg" variant="primary">Large</BButton>
            <BButton disabled variant="primary">Disabled</BButton>
          </div>
        </div>
      </div>
      <CodeBlock
        :code="BUTTON_CODE"
        collapsible
        :default-open="false"
        filename="Button.vue"
        language="vue"
      />
    </section>

    <hr class="my-2" />

    <!-- Input -->
    <section id="input">
      <div class="mb-2 ps-3" style="border-left: 3px solid var(--bs-primary)">
        <h5 class="mb-1">Input</h5>
        <p class="text-body-secondary mb-0 small">
          A single-line text field. Compose with
          <code>BFormGroup</code> and <code>BFormText</code> for accessible labelling.
        </p>
      </div>
      <div class="p-4 rounded-3 border bg-body-tertiary mb-3 shadow-sm">
        <div style="max-width: 24rem">
          <BFormGroup id="email-demo-group" label="Email address" label-for="email-demo">
            <BFormInput
              id="email-demo"
              placeholder="you@example.com"
              type="email"
            />
            <BFormText>We'll never share your email.</BFormText>
          </BFormGroup>
          <BFormGroup
            class="mt-3"
            id="disabled-input-group"
            label="Disabled"
            label-for="disabled-input"
          >
            <BFormInput disabled id="disabled-input" placeholder="Not editable" />
          </BFormGroup>
          <BFormGroup
            class="mt-3"
            id="error-input-group"
            label="With error"
            label-for="error-input"
            invalid-feedback="Please enter a valid value."
          >
            <BFormInput id="error-input" :state="false" value="bad input" />
          </BFormGroup>
        </div>
      </div>
      <CodeBlock
        :code="INPUT_CODE"
        collapsible
        :default-open="false"
        filename="Input.vue"
        language="vue"
      />
    </section>

    <hr class="my-2" />

    <!-- Textarea -->
    <section id="textarea">
      <div class="mb-2 ps-3" style="border-left: 3px solid var(--bs-primary)">
        <h5 class="mb-1">Textarea</h5>
        <p class="text-body-secondary mb-0 small">
          A multi-line text input. Compose with <code>BFormGroup</code> for accessible labelling.
        </p>
      </div>
      <div class="p-4 rounded-3 border bg-body-tertiary mb-3 shadow-sm">
        <div style="max-width: 24rem">
          <BFormGroup id="bio-demo-group" label="Bio" label-for="bio-demo">
            <BFormTextarea id="bio-demo" placeholder="Tell us about yourself…" :rows="4" />
            <BFormText>Describe your project in a few sentences.</BFormText>
          </BFormGroup>
        </div>
      </div>
      <CodeBlock
        :code="TEXTAREA_CODE"
        collapsible
        :default-open="false"
        filename="Textarea.vue"
        language="vue"
      />
    </section>

    <hr class="my-2" />

    <!-- Select -->
    <section id="select">
      <div class="mb-2 ps-3" style="border-left: 3px solid var(--bs-primary)">
        <h5 class="mb-1">Select</h5>
        <p class="text-body-secondary mb-0 small">
          A native dropdown using <code>BFormSelect</code> with an <code>:options</code> array.
        </p>
      </div>
      <div class="p-4 rounded-3 border bg-body-tertiary mb-3 shadow-sm">
        <div style="max-width: 24rem">
          <BFormGroup id="framework-demo-group" label="Framework" label-for="framework-demo">
            <BFormSelect
              id="framework-demo"
              :options="[
                { value: null, text: 'Select a framework…', disabled: true },
                { value: 'nextjs', text: 'Next.js' },
                { value: 'remix', text: 'Remix' },
                { value: 'astro', text: 'Astro' },
                { value: 'sveltekit', text: 'SvelteKit' },
              ]"
            />
          </BFormGroup>
          <BFormGroup
            class="mt-3"
            id="disabled-select-group"
            label="Disabled"
            label-for="disabled-select"
          >
            <BFormSelect
              disabled
              id="disabled-select"
              :options="[{ value: null, text: 'Not selectable' }]"
            />
          </BFormGroup>
        </div>
      </div>
      <CodeBlock
        :code="SELECT_CODE"
        collapsible
        :default-open="false"
        filename="Select.vue"
        language="vue"
      />
    </section>

    <hr class="my-2" />

    <!-- Checkbox -->
    <section id="checkbox">
      <div class="mb-2 ps-3" style="border-left: 3px solid var(--bs-primary)">
        <h5 class="mb-1">Checkbox</h5>
        <p class="text-body-secondary mb-0 small">
          A binary toggle using <code>BFormCheckbox</code> with <code>v-model</code>.
        </p>
      </div>
      <div class="p-4 rounded-3 border bg-body-tertiary mb-3 shadow-sm">
        <div class="d-flex flex-column gap-2">
          <BFormCheckbox v-model="checked">Accept terms and conditions</BFormCheckbox>
          <BFormCheckbox v-model="checkedDefault">Checked by default</BFormCheckbox>
          <BFormCheckbox disabled>Disabled</BFormCheckbox>
        </div>
      </div>
      <CodeBlock
        :code="CHECKBOX_CODE"
        collapsible
        :default-open="false"
        filename="Checkbox.vue"
        language="vue"
      />
    </section>

    <hr class="my-2" />

    <!-- RadioGroup -->
    <section id="radio-group">
      <div class="mb-2 ps-3" style="border-left: 3px solid var(--bs-primary)">
        <h5 class="mb-1">RadioGroup</h5>
        <p class="text-body-secondary mb-0 small">
          Mutually exclusive options using <code>BFormRadioGroup</code> with an
          <code>:options</code> array and <code>v-model</code>.
        </p>
      </div>
      <div class="p-4 rounded-3 border bg-body-tertiary mb-3 shadow-sm">
        <BFormRadioGroup
          v-model="plan"
          :options="[
            { value: 'free', text: 'Free' },
            { value: 'pro', text: 'Pro' },
            { value: 'enterprise', text: 'Enterprise' },
          ]"
          stacked
        />
      </div>
      <CodeBlock
        :code="RADIO_GROUP_CODE"
        collapsible
        :default-open="false"
        filename="RadioGroup.vue"
        language="vue"
      />
    </section>

    <hr class="my-2" />

    <!-- Switch -->
    <section id="switch">
      <div class="mb-2 ps-3" style="border-left: 3px solid var(--bs-primary)">
        <h5 class="mb-1">Switch</h5>
        <p class="text-body-secondary mb-0 small">
          An on/off toggle using <code>BFormCheckbox</code> with the
          <code>:switch="true"</code> prop.
        </p>
      </div>
      <div class="p-4 rounded-3 border bg-body-tertiary mb-3 shadow-sm">
        <div class="d-flex flex-column gap-2">
          <BFormCheckbox :switch="true">Email notifications</BFormCheckbox>
          <BFormCheckbox :switch="true" :model-value="true">Marketing emails</BFormCheckbox>
          <BFormCheckbox :switch="true" disabled>Disabled</BFormCheckbox>
        </div>
      </div>
      <CodeBlock
        :code="SWITCH_CODE"
        collapsible
        :default-open="false"
        filename="Switch.vue"
        language="vue"
      />
    </section>
  </div>
</template>
