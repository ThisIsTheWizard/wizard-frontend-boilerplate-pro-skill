<script setup lang="ts">
import { ref } from "vue";
import {
  Dialog,
  DialogPanel,
  DialogTitle,
  Popover,
  PopoverButton,
  PopoverPanel,
  TransitionChild,
  TransitionRoot,
} from "@headlessui/vue";

import CodeBlock from "@/components/ui/CodeBlock.vue";

const ALERT_CODE = `<template>
  <div class="flex flex-col gap-3">
    <div role="alert" class="flex items-start gap-3 rounded-lg border border-blue-200 bg-blue-50 px-4 py-3 text-sm text-blue-800 dark:border-blue-800 dark:bg-blue-950/30 dark:text-blue-300">
      <span class="mt-0.5 shrink-0">ℹ</span>
      <div><p class="font-medium">Info</p><p class="mt-0.5 opacity-80">Your session expires in 30 minutes.</p></div>
    </div>
    <div role="alert" class="flex items-start gap-3 rounded-lg border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-800 dark:border-green-800 dark:bg-green-950/30 dark:text-green-300">
      <span class="mt-0.5 shrink-0">✓</span>
      <div><p class="font-medium">Success</p><p class="mt-0.5 opacity-80">Your changes have been saved.</p></div>
    </div>
  </div>
</template>`;

const TOAST_CODE = `<script setup lang="ts">
import { ref } from "vue";

let nextId = 0;
const toasts = ref<Array<{ id: number; message: string }>>([]);

function showToast(message: string) {
  const id = ++nextId;
  toasts.value.push({ id, message });
  setTimeout(() => { toasts.value = toasts.value.filter((t) => t.id !== id); }, 3000);
}
<\/script>

<template>
  <button @click="showToast('Action completed!')" class="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:opacity-90">
    Show toast
  </button>
  <teleport to="body">
    <div class="fixed bottom-4 right-4 z-50 flex flex-col gap-2">
      <div v-for="t in toasts" :key="t.id" class="rounded-lg border border-border bg-surface px-4 py-3 text-sm text-foreground shadow-lg">
        {{ t.message }}
      </div>
    </div>
  </teleport>
</template>`;

const PROGRESS_CODE = `<template>
  <div>
    <div class="mb-1 flex items-center justify-between text-sm">
      <span class="text-foreground">Upload progress</span>
      <span class="text-muted-foreground">72%</span>
    </div>
    <div class="h-2 w-full overflow-hidden rounded-full bg-muted">
      <div class="h-full w-[72%] rounded-full bg-primary transition-all" />
    </div>
  </div>
</template>`;

const TOOLTIP_CODE = `<script setup lang="ts">
import { Popover, PopoverButton, PopoverPanel } from "@headlessui/vue";
<\/script>

<template>
  <Popover class="relative inline-block">
    <PopoverButton class="rounded-md border border-border px-3 py-1.5 text-sm hover:bg-accent focus:outline-none">
      Hover / click me
    </PopoverButton>
    <PopoverPanel anchor="top" class="z-50 max-w-xs rounded-md bg-foreground px-3 py-1.5 text-xs text-background shadow-md [--anchor-gap:6px]">
      This is a tooltip built with Headless UI Popover.
    </PopoverPanel>
  </Popover>
</template>`;

const DIALOG_CODE = `<script setup lang="ts">
import { Dialog, DialogPanel, DialogTitle, TransitionRoot, TransitionChild } from "@headlessui/vue";
import { ref } from "vue";

const open = ref(false);
<\/script>

<template>
  <button @click="open = true" class="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:opacity-90">Open dialog</button>
  <TransitionRoot :show="open" as="template">
    <Dialog @close="open = false">
      <TransitionChild enter="ease-out duration-200" enter-from="opacity-0" enter-to="opacity-100" leave="ease-in duration-150" leave-from="opacity-100" leave-to="opacity-0">
        <div class="fixed inset-0 z-40 bg-black/50" />
      </TransitionChild>
      <div class="fixed inset-0 z-50 flex items-center justify-center p-4">
        <TransitionChild enter="ease-out duration-200" enter-from="opacity-0 scale-95" enter-to="opacity-100 scale-100" leave="ease-in duration-150" leave-from="opacity-100 scale-100" leave-to="opacity-0 scale-95">
          <DialogPanel class="w-full max-w-md rounded-xl border border-border bg-surface p-6 shadow-xl">
            <DialogTitle class="text-lg font-semibold text-foreground">Delete project?</DialogTitle>
            <p class="mt-2 text-sm text-muted-foreground">This action cannot be undone.</p>
            <div class="mt-6 flex justify-end gap-3">
              <button @click="open = false" class="rounded-md border border-border px-4 py-2 text-sm font-medium text-foreground hover:bg-accent">Cancel</button>
              <button @click="open = false" class="rounded-md bg-destructive px-4 py-2 text-sm font-medium text-white hover:opacity-90">Delete</button>
            </div>
          </DialogPanel>
        </TransitionChild>
      </div>
    </Dialog>
  </TransitionRoot>
</template>`;

let nextToastId = 0;
const toasts = ref<Array<{ id: number; message: string }>>([]);
const dialogOpen = ref(false);

function showToast(message: string) {
  const id = ++nextToastId;
  toasts.value.push({ id, message });
  setTimeout(() => {
    toasts.value = toasts.value.filter((t) => t.id !== id);
  }, 3000);
}
</script>

<template>
  <div class="flex flex-col gap-16">
    <div>
      <h1 class="text-3xl font-bold tracking-tight text-foreground">Feedback</h1>
      <p class="mt-2 text-muted-foreground">
        Status indicators and interactive feedback — Dialog and Popover from Headless UI; Alert, Toast, and Progress are pure Tailwind.
      </p>
    </div>

    <section class="flex flex-col gap-4" id="alert">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Alert</h2>
        <p class="mt-1 text-sm text-muted-foreground">Inline status messages using <code class="rounded bg-muted px-1 py-0.5 text-xs">role="alert"</code> with semantic colour variants.</p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <div class="flex flex-col gap-3">
          <div role="alert" class="flex items-start gap-3 rounded-lg border border-blue-200 bg-blue-50 px-4 py-3 text-sm text-blue-800 dark:border-blue-800 dark:bg-blue-950/30 dark:text-blue-300">
            <span class="mt-0.5 shrink-0">ℹ</span>
            <div><p class="font-medium">Info</p><p class="mt-0.5 opacity-80">Your session expires in 30 minutes.</p></div>
          </div>
          <div role="alert" class="flex items-start gap-3 rounded-lg border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-800 dark:border-green-800 dark:bg-green-950/30 dark:text-green-300">
            <span class="mt-0.5 shrink-0">✓</span>
            <div><p class="font-medium">Success</p><p class="mt-0.5 opacity-80">Your changes have been saved.</p></div>
          </div>
          <div role="alert" class="flex items-start gap-3 rounded-lg border border-yellow-200 bg-yellow-50 px-4 py-3 text-sm text-yellow-800 dark:border-yellow-800 dark:bg-yellow-950/30 dark:text-yellow-300">
            <span class="mt-0.5 shrink-0">⚠</span>
            <div><p class="font-medium">Warning</p><p class="mt-0.5 opacity-80">This will affect all team members.</p></div>
          </div>
          <div role="alert" class="flex items-start gap-3 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-800 dark:border-red-800 dark:bg-red-950/30 dark:text-red-300">
            <span class="mt-0.5 shrink-0">✕</span>
            <div><p class="font-medium">Error</p><p class="mt-0.5 opacity-80">Something went wrong. Please try again.</p></div>
          </div>
        </div>
      </div>
      <CodeBlock :code="ALERT_CODE" :collapsible="true" :default-open="false" filename="Alert.vue" language="vue" />
    </section>

    <section class="flex flex-col gap-4" id="toast">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Toast</h2>
        <p class="mt-1 text-sm text-muted-foreground">Ephemeral notification queue using <code class="rounded bg-muted px-1 py-0.5 text-xs">ref</code> array auto-dismissed after 3 seconds.</p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <button class="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition hover:opacity-90" type="button" @click="showToast('Action completed successfully!')">
          Show toast
        </button>
      </div>
      <CodeBlock :code="TOAST_CODE" :collapsible="true" :default-open="false" filename="Toast.vue" language="vue" />
    </section>

    <section class="flex flex-col gap-4" id="progress">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Progress</h2>
        <p class="mt-1 text-sm text-muted-foreground">A determinate progress bar using a filled <code class="rounded bg-muted px-1 py-0.5 text-xs">div</code> inside a track.</p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <div class="flex max-w-sm flex-col gap-6">
          <div>
            <div class="mb-1 flex items-center justify-between text-sm">
              <span class="text-foreground">Upload progress</span>
              <span class="text-muted-foreground">72%</span>
            </div>
            <div class="h-2 w-full overflow-hidden rounded-full bg-muted">
              <div class="h-full w-[72%] rounded-full bg-primary transition-all" />
            </div>
          </div>
          <div>
            <div class="mb-1 flex items-center justify-between text-sm">
              <span class="text-foreground">Storage used</span>
              <span class="text-muted-foreground">40%</span>
            </div>
            <div class="h-2 w-full overflow-hidden rounded-full bg-muted">
              <div class="h-full w-[40%] rounded-full bg-primary/60 transition-all" />
            </div>
          </div>
        </div>
      </div>
      <CodeBlock :code="PROGRESS_CODE" :collapsible="true" :default-open="false" filename="Progress.vue" language="vue" />
    </section>

    <section class="flex flex-col gap-4" id="tooltip">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Tooltip</h2>
        <p class="mt-1 text-sm text-muted-foreground">Contextual hint using Headless UI <code class="rounded bg-muted px-1 py-0.5 text-xs">Popover</code> anchored above its trigger.</p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <Popover class="relative inline-block">
          <PopoverButton class="rounded-md border border-border px-3 py-1.5 text-sm transition hover:bg-accent focus:outline-none">
            Hover / click me
          </PopoverButton>
          <PopoverPanel anchor="top" class="z-50 max-w-xs rounded-md bg-foreground px-3 py-1.5 text-xs text-background shadow-md [--anchor-gap:6px]">
            This is a tooltip built with Headless UI Popover.
          </PopoverPanel>
        </Popover>
      </div>
      <CodeBlock :code="TOOLTIP_CODE" :collapsible="true" :default-open="false" filename="Tooltip.vue" language="vue" />
    </section>

    <section class="flex flex-col gap-4" id="dialog">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Dialog</h2>
        <p class="mt-1 text-sm text-muted-foreground">Headless UI <code class="rounded bg-muted px-1 py-0.5 text-xs">Dialog</code> + <code class="rounded bg-muted px-1 py-0.5 text-xs">TransitionRoot</code> — manages focus trap, scroll lock, and ARIA.</p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <button class="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition hover:opacity-90" type="button" @click="dialogOpen = true">
          Open dialog
        </button>
        <TransitionRoot :show="dialogOpen" as="template">
          <Dialog @close="dialogOpen = false">
            <TransitionChild
              enter="ease-out duration-200"
              enter-from="opacity-0"
              enter-to="opacity-100"
              leave="ease-in duration-150"
              leave-from="opacity-100"
              leave-to="opacity-0"
            >
              <div class="fixed inset-0 z-40 bg-black/50" />
            </TransitionChild>
            <div class="fixed inset-0 z-50 flex items-center justify-center p-4">
              <TransitionChild
                enter="ease-out duration-200"
                enter-from="opacity-0 scale-95"
                enter-to="opacity-100 scale-100"
                leave="ease-in duration-150"
                leave-from="opacity-100 scale-100"
                leave-to="opacity-0 scale-95"
              >
                <DialogPanel class="w-full max-w-md rounded-xl border border-border bg-surface p-6 shadow-xl">
                  <DialogTitle class="text-lg font-semibold text-foreground">Delete project?</DialogTitle>
                  <p class="mt-2 text-sm text-muted-foreground">This action cannot be undone. All data will be permanently removed.</p>
                  <div class="mt-6 flex justify-end gap-3">
                    <button class="rounded-md border border-border px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent" type="button" @click="dialogOpen = false">Cancel</button>
                    <button class="rounded-md bg-destructive px-4 py-2 text-sm font-medium text-white transition hover:opacity-90" type="button" @click="dialogOpen = false">Delete</button>
                  </div>
                </DialogPanel>
              </TransitionChild>
            </div>
          </Dialog>
        </TransitionRoot>
      </div>
      <CodeBlock :code="DIALOG_CODE" :collapsible="true" :default-open="false" filename="Dialog.vue" language="vue" />
    </section>
  </div>

  <teleport to="body">
    <div v-if="toasts.length > 0" class="fixed bottom-4 right-4 z-50 flex flex-col gap-2">
      <div
        v-for="t in toasts"
        :key="t.id"
        class="rounded-lg border border-border bg-surface px-4 py-3 text-sm text-foreground shadow-lg"
      >
        {{ t.message }}
      </div>
    </div>
  </teleport>
</template>
