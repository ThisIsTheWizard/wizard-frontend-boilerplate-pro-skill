<script setup lang="ts">
import { ref } from "vue";
import {
  Dialog,
  DialogPanel,
  DialogTitle,
  Menu,
  MenuButton,
  MenuItem,
  MenuItems,
  Popover,
  PopoverButton,
  PopoverPanel,
  TransitionChild,
  TransitionRoot,
} from "@headlessui/vue";

import CodeBlock from "@/components/ui/CodeBlock.vue";

const POPOVER_CODE = `<script setup lang="ts">
import { Popover, PopoverButton, PopoverPanel } from "@headlessui/vue";
<\/script>

<template>
  <Popover class="relative">
    <PopoverButton class="rounded-md border border-border px-3 py-1.5 text-sm font-medium text-foreground hover:bg-accent focus:outline-none">
      Open Popover
    </PopoverButton>
    <PopoverPanel anchor="bottom start" class="z-50 w-64 rounded-lg border border-border bg-surface p-4 shadow-lg [--anchor-gap:4px]">
      <p class="text-sm font-medium text-foreground">Quick info</p>
      <p class="mt-1 text-sm text-muted-foreground">This is a popover panel with contextual information or actions.</p>
    </PopoverPanel>
  </Popover>
</template>`;

const DROPDOWN_CODE = `<script setup lang="ts">
import { Menu, MenuButton, MenuItem, MenuItems } from "@headlessui/vue";
<\/script>

<template>
  <Menu as="div" class="relative">
    <MenuButton class="rounded-md border border-border px-3 py-1.5 text-sm font-medium text-foreground hover:bg-accent focus:outline-none">
      Options ▾
    </MenuButton>
    <MenuItems class="absolute right-0 z-50 mt-1 min-w-40 rounded-lg border border-border bg-surface p-1 shadow-lg focus:outline-none">
      <div class="px-2 py-1.5 text-xs font-semibold text-muted-foreground">My Account</div>
      <div class="my-1 h-px bg-border" />
      <MenuItem v-slot="{ active }">
        <button :class="['block w-full rounded-md px-3 py-1.5 text-left text-sm text-foreground', active ? 'bg-accent' : '']">Profile</button>
      </MenuItem>
      <MenuItem v-slot="{ active }">
        <button :class="['block w-full rounded-md px-3 py-1.5 text-left text-sm text-foreground', active ? 'bg-accent' : '']">Settings</button>
      </MenuItem>
      <div class="my-1 h-px bg-border" />
      <MenuItem v-slot="{ active }">
        <button :class="['block w-full rounded-md px-3 py-1.5 text-left text-sm text-destructive', active ? 'bg-accent' : '']">Sign out</button>
      </MenuItem>
    </MenuItems>
  </Menu>
</template>`;

const SHEET_CODE = `<script setup lang="ts">
import { Dialog, DialogPanel, DialogTitle, TransitionRoot, TransitionChild } from "@headlessui/vue";
import { ref } from "vue";

const open = ref(false);
<\/script>

<template>
  <button @click="open = true" class="rounded-md border border-border px-4 py-2 text-sm font-medium text-foreground hover:bg-accent">Open Sheet</button>
  <TransitionRoot :show="open" as="template">
    <Dialog @close="open = false">
      <TransitionChild enter="ease-out duration-200" enter-from="opacity-0" enter-to="opacity-100" leave="ease-in duration-150" leave-from="opacity-100" leave-to="opacity-0">
        <div class="fixed inset-0 z-40 bg-black/50" />
      </TransitionChild>
      <div class="fixed inset-y-0 right-0 z-50 flex">
        <TransitionChild enter="ease-out duration-200" enter-from="translate-x-full" enter-to="translate-x-0" leave="ease-in duration-150" leave-from="translate-x-0" leave-to="translate-x-full">
          <DialogPanel class="relative flex w-80 flex-col border-l border-border bg-surface p-6 shadow-xl">
            <button @click="open = false" class="absolute right-4 top-4 rounded p-1 text-muted-foreground hover:bg-accent" aria-label="Close">✕</button>
            <DialogTitle class="text-lg font-semibold text-foreground">Edit Profile</DialogTitle>
            <p class="mt-2 text-sm text-muted-foreground">Make changes to your profile here.</p>
            <div class="mt-auto flex justify-end">
              <button @click="open = false" class="rounded-md border border-border px-4 py-2 text-sm font-medium text-foreground hover:bg-accent">Close</button>
            </div>
          </DialogPanel>
        </TransitionChild>
      </div>
    </Dialog>
  </TransitionRoot>
</template>`;

const sheetOpen = ref(false);
</script>

<template>
  <div class="flex flex-col gap-16">
    <div>
      <h1 class="text-3xl font-bold tracking-tight text-foreground">Overlay</h1>
      <p class="mt-2 text-muted-foreground">
        Floating and slide-over components — all powered by Headless UI <code class="rounded bg-muted px-1 py-0.5 text-xs">Popover</code>, <code class="rounded bg-muted px-1 py-0.5 text-xs">Menu</code>, and <code class="rounded bg-muted px-1 py-0.5 text-xs">Dialog</code>.
      </p>
    </div>

    <section class="flex flex-col gap-4" id="popover">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Popover</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          Headless UI <code class="rounded bg-muted px-1 py-0.5 text-xs">Popover</code> with <code class="rounded bg-muted px-1 py-0.5 text-xs">anchor</code> prop — manages focus and keyboard dismissal.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <Popover class="relative">
          <PopoverButton class="rounded-md border border-border px-3 py-1.5 text-sm font-medium text-foreground transition hover:bg-accent focus:outline-none">
            Open Popover
          </PopoverButton>
          <PopoverPanel anchor="bottom start" class="z-50 w-64 rounded-lg border border-border bg-surface p-4 shadow-lg [--anchor-gap:4px]">
            <p class="text-sm font-medium text-foreground">Quick info</p>
            <p class="mt-1 text-sm text-muted-foreground">This is a popover panel with contextual information or actions.</p>
          </PopoverPanel>
        </Popover>
      </div>
      <CodeBlock :code="POPOVER_CODE" :collapsible="true" :default-open="false" filename="Popover.vue" language="vue" />
    </section>

    <section class="flex flex-col gap-4" id="dropdown-menu">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">DropdownMenu</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          Headless UI <code class="rounded bg-muted px-1 py-0.5 text-xs">Menu</code> — roving focus list with <code class="rounded bg-muted px-1 py-0.5 text-xs">v-slot="{ active }"</code> styling.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <Menu as="div" class="relative">
          <MenuButton class="rounded-md border border-border px-3 py-1.5 text-sm font-medium text-foreground transition hover:bg-accent focus:outline-none">
            Options ▾
          </MenuButton>
          <MenuItems class="absolute right-0 z-50 mt-1 min-w-40 rounded-lg border border-border bg-surface p-1 shadow-lg focus:outline-none">
            <div class="px-2 py-1.5 text-xs font-semibold text-muted-foreground">My Account</div>
            <div class="my-1 h-px bg-border" />
            <MenuItem v-slot="{ active }">
              <button :class="['block w-full rounded-md px-3 py-1.5 text-left text-sm text-foreground', active ? 'bg-accent' : '']" type="button">Profile</button>
            </MenuItem>
            <MenuItem v-slot="{ active }">
              <button :class="['block w-full rounded-md px-3 py-1.5 text-left text-sm text-foreground', active ? 'bg-accent' : '']" type="button">Settings</button>
            </MenuItem>
            <div class="my-1 h-px bg-border" />
            <MenuItem v-slot="{ active }">
              <button :class="['block w-full rounded-md px-3 py-1.5 text-left text-sm text-destructive', active ? 'bg-accent' : '']" type="button">Sign out</button>
            </MenuItem>
          </MenuItems>
        </Menu>
      </div>
      <CodeBlock :code="DROPDOWN_CODE" :collapsible="true" :default-open="false" filename="DropdownMenu.vue" language="vue" />
    </section>

    <section class="flex flex-col gap-4" id="sheet">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Sheet</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          A slide-over panel using Headless UI <code class="rounded bg-muted px-1 py-0.5 text-xs">Dialog</code> + <code class="rounded bg-muted px-1 py-0.5 text-xs">TransitionChild</code> with <code class="rounded bg-muted px-1 py-0.5 text-xs">translate-x</code> enter/leave.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <button class="rounded-md border border-border px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent" type="button" @click="sheetOpen = true">
          Open Sheet
        </button>
        <TransitionRoot :show="sheetOpen" as="template">
          <Dialog @close="sheetOpen = false">
            <TransitionChild
              enter="ease-out duration-200"
              enter-from="opacity-0"
              enter-to="opacity-100"
              leave="ease-in duration-150"
              leave-from="opacity-100"
              leave-to="opacity-0"
            >
              <div class="fixed inset-0 z-40 bg-black/50" />
            </TransitionChild>
            <div class="fixed inset-y-0 right-0 z-50 flex">
              <TransitionChild
                enter="ease-out duration-200"
                enter-from="translate-x-full"
                enter-to="translate-x-0"
                leave="ease-in duration-150"
                leave-from="translate-x-0"
                leave-to="translate-x-full"
              >
                <DialogPanel class="relative flex w-80 flex-col border-l border-border bg-surface p-6 shadow-xl">
                  <button
                    aria-label="Close"
                    class="absolute right-4 top-4 rounded p-1 text-muted-foreground transition hover:bg-accent"
                    type="button"
                    @click="sheetOpen = false"
                  >
                    ✕
                  </button>
                  <DialogTitle class="text-lg font-semibold text-foreground">Edit Profile</DialogTitle>
                  <p class="mt-2 text-sm text-muted-foreground">Make changes to your profile here. Click close when you are done.</p>
                  <div class="mt-auto flex justify-end">
                    <button class="rounded-md border border-border px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent" type="button" @click="sheetOpen = false">
                      Close
                    </button>
                  </div>
                </DialogPanel>
              </TransitionChild>
            </div>
          </Dialog>
        </TransitionRoot>
      </div>
      <CodeBlock :code="SHEET_CODE" :collapsible="true" :default-open="false" filename="Sheet.vue" language="vue" />
    </section>
  </div>
</template>
