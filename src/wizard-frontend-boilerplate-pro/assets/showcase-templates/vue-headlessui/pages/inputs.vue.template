<script setup lang="ts">
import { ref } from "vue";
import {
  Listbox,
  ListboxButton,
  ListboxOption,
  ListboxOptions,
  RadioGroup,
  RadioGroupOption,
  Switch,
} from "@headlessui/vue";

import CodeBlock from "@/components/ui/CodeBlock.vue";
import { cn } from "@/lib/utils";

const BUTTON_CODE = `<template>
  <div class="flex flex-wrap gap-2">
    <button class="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:opacity-90">Primary</button>
    <button class="rounded-md border border-border bg-background px-4 py-2 text-sm font-medium text-foreground hover:bg-accent">Secondary</button>
    <button class="rounded-md border border-border px-4 py-2 text-sm font-medium text-foreground hover:bg-accent">Outline</button>
    <button class="rounded-md px-4 py-2 text-sm font-medium text-foreground hover:bg-accent">Ghost</button>
    <button class="rounded-md bg-destructive px-4 py-2 text-sm font-medium text-white hover:opacity-90">Destructive</button>
  </div>
</template>`;

const INPUT_CODE = `<template>
  <div class="flex flex-col gap-1.5">
    <label for="email" class="text-sm font-medium text-foreground">Email address</label>
    <input
      id="email"
      type="email"
      placeholder="you@example.com"
      class="rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-ring"
    />
    <p class="text-sm text-muted-foreground">We'll never share your email.</p>
  </div>
</template>`;

const TEXTAREA_CODE = `<template>
  <div class="flex flex-col gap-1.5">
    <label for="bio" class="text-sm font-medium text-foreground">Bio</label>
    <textarea
      id="bio"
      :rows="4"
      placeholder="Tell us about yourself…"
      class="rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-ring"
    />
    <p class="text-sm text-muted-foreground">Describe your project in a few sentences.</p>
  </div>
</template>`;

const SELECT_CODE = `<script setup lang="ts">
import { Listbox, ListboxButton, ListboxOption, ListboxOptions } from "@headlessui/vue";
import { ref } from "vue";

const options = ["Next.js", "Remix", "Astro", "SvelteKit"];
const selected = ref(options[0]);
<\/script>

<template>
  <Listbox v-model="selected">
    <div class="relative">
      <ListboxButton class="relative w-full rounded-md border border-border bg-background px-3 py-2 text-left text-sm focus:outline-none focus:ring-2 focus:ring-ring">
        {{ selected }}
        <span class="pointer-events-none absolute inset-y-0 right-3 flex items-center text-muted-foreground">▾</span>
      </ListboxButton>
      <ListboxOptions class="absolute z-50 mt-1 w-full rounded-md border border-border bg-surface shadow-lg focus:outline-none">
        <ListboxOption v-for="opt in options" :key="opt" :value="opt" v-slot="{ active, selected: sel }">
          <span :class="['block cursor-pointer px-3 py-2 text-sm text-foreground', active ? 'bg-accent' : '', sel ? 'font-medium text-primary' : '']">{{ opt }}</span>
        </ListboxOption>
      </ListboxOptions>
    </div>
  </Listbox>
</template>`;

const CHECKBOX_CODE = `<script setup lang="ts">
import { ref } from "vue";
const checked = ref(false);
<\/script>

<template>
  <label class="flex cursor-pointer items-center gap-2">
    <input
      v-model="checked"
      type="checkbox"
      class="h-4 w-4 rounded border border-border text-primary accent-primary"
    />
    <span class="text-sm text-foreground">Accept terms and conditions</span>
  </label>
</template>`;

const RADIO_CODE = `<script setup lang="ts">
import { RadioGroup, RadioGroupOption } from "@headlessui/vue";
import { ref } from "vue";

const plan = ref("Free");
const plans = ["Free", "Pro", "Enterprise"];
<\/script>

<template>
  <RadioGroup v-model="plan" class="flex flex-col gap-2">
    <RadioGroupOption v-for="p in plans" :key="p" :value="p" v-slot="{ checked }">
      <div class="flex cursor-pointer items-center gap-2">
        <span :class="['flex h-4 w-4 items-center justify-center rounded-full border', checked ? 'border-primary' : 'border-border']">
          <span v-if="checked" class="h-2 w-2 rounded-full bg-primary" />
        </span>
        <span class="text-sm text-foreground">{{ p }}</span>
      </div>
    </RadioGroupOption>
  </RadioGroup>
</template>`;

const SWITCH_CODE = `<script setup lang="ts">
import { Switch } from "@headlessui/vue";
import { ref } from "vue";

const enabled = ref(false);
<\/script>

<template>
  <label class="flex cursor-pointer items-center gap-2">
    <Switch
      v-model="enabled"
      :class="['relative inline-flex h-6 w-11 items-center rounded-full transition', enabled ? 'bg-primary' : 'bg-muted-foreground/30']"
    >
      <span :class="['inline-block h-4 w-4 rounded-full bg-white transition', enabled ? 'translate-x-6' : 'translate-x-1']" />
    </Switch>
    <span class="text-sm text-foreground">Email notifications</span>
  </label>
</template>`;

const FRAMEWORKS = ["Next.js", "Remix", "Astro", "SvelteKit"];
const PLANS = ["Free", "Pro", "Enterprise"];

const selected = ref(FRAMEWORKS[0]);
const plan = ref("Free");
const termsChecked = ref(false);
const defaultChecked = ref(true);
const notifications = ref(false);
const marketing = ref(true);
</script>

<template>
  <div class="flex flex-col gap-16">
    <div>
      <h1 class="text-3xl font-bold tracking-tight text-foreground">Inputs</h1>
      <p class="mt-2 text-base leading-relaxed text-muted-foreground">
        Form controls using Headless UI primitives (Listbox, RadioGroup, Switch) and native HTML
        inputs styled with Tailwind.
      </p>
    </div>

    <section class="flex flex-col gap-4" id="button">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">Button</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          Plain HTML buttons — Headless UI has no Button primitive; use the native element directly.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <div class="flex flex-col gap-4">
          <div class="flex flex-wrap gap-2">
            <button class="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition hover:opacity-90">Primary</button>
            <button class="rounded-md border border-border bg-background px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent">Secondary</button>
            <button class="rounded-md border border-border px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent">Outline</button>
            <button class="rounded-md px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent">Ghost</button>
            <button class="rounded-md bg-destructive px-4 py-2 text-sm font-medium text-white transition hover:opacity-90">Destructive</button>
          </div>
          <div class="flex flex-wrap items-center gap-2">
            <button class="rounded-md bg-primary px-3 py-1.5 text-xs font-medium text-primary-foreground transition hover:opacity-90">Small</button>
            <button class="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition hover:opacity-90">Default</button>
            <button class="rounded-md bg-primary px-5 py-2.5 text-base font-medium text-primary-foreground transition hover:opacity-90">Large</button>
            <button disabled class="cursor-not-allowed rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground opacity-50">Disabled</button>
          </div>
        </div>
      </div>
      <CodeBlock :code="BUTTON_CODE" :collapsible="true" :default-open="false" filename="Button.vue" language="vue" />
    </section>

    <section class="flex flex-col gap-4" id="input">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">Input</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          Native <code class="rounded bg-muted px-1 py-0.5 text-xs">input</code> with Tailwind — Headless UI v1 (Vue) has no Input primitive.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <div class="flex max-w-sm flex-col gap-4">
          <div class="flex flex-col gap-1.5">
            <label class="text-sm font-medium text-foreground" for="email-demo">Email address</label>
            <input id="email-demo" class="rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-ring" placeholder="you@example.com" type="email" />
            <p class="text-sm text-muted-foreground">We'll never share your email.</p>
          </div>
          <div class="flex flex-col gap-1.5">
            <label class="text-sm font-medium text-foreground" for="username-demo">Username</label>
            <input aria-invalid="true" class="rounded-md border border-destructive bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-destructive" id="username-demo" value="invalid user!" />
            <p class="text-sm text-destructive">Please enter a valid username.</p>
          </div>
          <div class="flex flex-col gap-1.5">
            <label class="text-sm font-medium text-foreground opacity-50" for="disabled-demo">Disabled</label>
            <input disabled class="cursor-not-allowed rounded-md border border-border bg-muted px-3 py-2 text-sm opacity-50 outline-none" id="disabled-demo" placeholder="Not editable" />
          </div>
        </div>
      </div>
      <CodeBlock :code="INPUT_CODE" :collapsible="true" :default-open="false" filename="Input.vue" language="vue" />
    </section>

    <section class="flex flex-col gap-4" id="textarea">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">Textarea</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          Native <code class="rounded bg-muted px-1 py-0.5 text-xs">textarea</code> with Tailwind.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <div class="flex max-w-sm flex-col gap-4">
          <div class="flex flex-col gap-1.5">
            <label class="text-sm font-medium text-foreground" for="bio-demo">Bio</label>
            <textarea class="rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-ring" id="bio-demo" :rows="4" placeholder="Tell us about yourself…" />
            <p class="text-sm text-muted-foreground">Describe your project in a few sentences.</p>
          </div>
          <div class="flex flex-col gap-1.5">
            <label class="text-sm font-medium text-foreground" for="message-demo">Message</label>
            <textarea aria-invalid="true" class="rounded-md border border-destructive bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-destructive" id="message-demo" placeholder="Enter your message…" />
            <p class="text-sm text-destructive">Message is required.</p>
          </div>
        </div>
      </div>
      <CodeBlock :code="TEXTAREA_CODE" :collapsible="true" :default-open="false" filename="Textarea.vue" language="vue" />
    </section>

    <section class="flex flex-col gap-4" id="select">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">Select</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          Headless UI <code class="rounded bg-muted px-1 py-0.5 text-xs">Listbox</code> — accessible dropdown with keyboard navigation.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <div class="flex max-w-sm flex-col gap-4">
          <div class="flex flex-col gap-1.5">
            <label class="text-sm font-medium text-foreground">Framework</label>
            <Listbox v-model="selected">
              <div class="relative">
                <ListboxButton class="relative w-full rounded-md border border-border bg-background px-3 py-2 text-left text-sm focus:outline-none focus:ring-2 focus:ring-ring">
                  {{ selected }}
                  <span class="pointer-events-none absolute inset-y-0 right-3 flex items-center text-muted-foreground">▾</span>
                </ListboxButton>
                <ListboxOptions class="absolute z-50 mt-1 w-full rounded-md border border-border bg-surface shadow-lg focus:outline-none">
                  <ListboxOption
                    v-for="opt in FRAMEWORKS"
                    :key="opt"
                    :value="opt"
                    v-slot="{ active, selected: isSel }"
                  >
                    <span :class="cn('block cursor-pointer px-3 py-2 text-sm text-foreground', active ? 'bg-accent' : '', isSel ? 'font-medium text-primary' : '')">
                      {{ opt }}
                    </span>
                  </ListboxOption>
                </ListboxOptions>
              </div>
            </Listbox>
          </div>
        </div>
      </div>
      <CodeBlock :code="SELECT_CODE" :collapsible="true" :default-open="false" filename="Select.vue" language="vue" />
    </section>

    <section class="flex flex-col gap-4" id="checkbox">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">Checkbox</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          Native <code class="rounded bg-muted px-1 py-0.5 text-xs">input[type=checkbox]</code> — Headless UI Vue v1 has no Checkbox primitive.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <div class="flex flex-col gap-3">
          <label class="flex cursor-pointer items-center gap-2">
            <input v-model="termsChecked" class="h-4 w-4 rounded border border-border accent-primary" type="checkbox" />
            <span class="text-sm text-foreground">Accept terms and conditions</span>
          </label>
          <label class="flex cursor-pointer items-center gap-2">
            <input v-model="defaultChecked" class="h-4 w-4 rounded border border-border accent-primary" type="checkbox" />
            <span class="text-sm text-foreground">Checked by default</span>
          </label>
          <label class="flex cursor-not-allowed items-center gap-2 opacity-50">
            <input class="h-4 w-4 rounded border border-border" disabled type="checkbox" />
            <span class="text-sm text-foreground">Disabled</span>
          </label>
        </div>
      </div>
      <CodeBlock :code="CHECKBOX_CODE" :collapsible="true" :default-open="false" filename="Checkbox.vue" language="vue" />
    </section>

    <section class="flex flex-col gap-4" id="radio-group">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">RadioGroup</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          Headless UI <code class="rounded bg-muted px-1 py-0.5 text-xs">RadioGroup</code> + <code class="rounded bg-muted px-1 py-0.5 text-xs">RadioGroupOption</code> with arrow-key navigation.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <RadioGroup v-model="plan" class="flex flex-col gap-2">
          <RadioGroupOption
            v-for="p in PLANS"
            :key="p"
            :value="p"
            v-slot="{ checked }"
          >
            <div class="flex cursor-pointer items-center gap-2 py-0.5">
              <span :class="['flex h-4 w-4 items-center justify-center rounded-full border', checked ? 'border-primary' : 'border-border']">
                <span v-if="checked" class="h-2 w-2 rounded-full bg-primary" />
              </span>
              <span class="text-sm text-foreground">{{ p }}</span>
            </div>
          </RadioGroupOption>
        </RadioGroup>
      </div>
      <CodeBlock :code="RADIO_CODE" :collapsible="true" :default-open="false" filename="RadioGroup.vue" language="vue" />
    </section>

    <section class="flex flex-col gap-4" id="switch">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">Switch</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          Headless UI <code class="rounded bg-muted px-1 py-0.5 text-xs">Switch</code> — toggle styled via <code class="rounded bg-muted px-1 py-0.5 text-xs">v-model</code> and <code class="rounded bg-muted px-1 py-0.5 text-xs">translate-x</code>.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <div class="flex flex-col gap-3">
          <label class="flex cursor-pointer items-center gap-2">
            <Switch
              v-model="notifications"
              :class="['relative inline-flex h-6 w-11 items-center rounded-full transition', notifications ? 'bg-primary' : 'bg-muted-foreground/30']"
            >
              <span :class="['inline-block h-4 w-4 rounded-full bg-white transition', notifications ? 'translate-x-6' : 'translate-x-1']" />
            </Switch>
            <span class="text-sm text-foreground">Email notifications</span>
          </label>
          <label class="flex cursor-pointer items-center gap-2">
            <Switch
              v-model="marketing"
              :class="['relative inline-flex h-6 w-11 items-center rounded-full transition', marketing ? 'bg-primary' : 'bg-muted-foreground/30']"
            >
              <span :class="['inline-block h-4 w-4 rounded-full bg-white transition', marketing ? 'translate-x-6' : 'translate-x-1']" />
            </Switch>
            <span class="text-sm text-foreground">Marketing emails</span>
          </label>
          <label class="flex cursor-not-allowed items-center gap-2 opacity-50">
            <Switch
              :model-value="false"
              class="relative inline-flex h-6 w-11 items-center rounded-full bg-muted-foreground/30"
              disabled
            >
              <span class="inline-block h-4 w-4 translate-x-1 rounded-full bg-white" />
            </Switch>
            <span class="text-sm text-foreground">Disabled</span>
          </label>
        </div>
      </div>
      <CodeBlock :code="SWITCH_CODE" :collapsible="true" :default-open="false" filename="Switch.vue" language="vue" />
    </section>
  </div>
</template>
