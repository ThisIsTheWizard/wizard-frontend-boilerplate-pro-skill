<script setup lang="ts">
import Avatar from "primevue/avatar";
import Button from "primevue/button";
import Card from "primevue/card";
import Column from "primevue/column";
import DataTable from "primevue/datatable";
import Divider from "primevue/divider";
import Skeleton from "primevue/skeleton";
import Tag from "primevue/tag";

import CodeBlock from "@/components/CodeBlock.vue";

const TABLE_ROWS = [
  { email: "alice@example.com", name: "Alice Martin", role: "Admin" },
  { email: "bob@example.com", name: "Bob Chen", role: "Editor" },
  { email: "carol@example.com", name: "Carol White", role: "Viewer" },
];

const CARD_CODE = `<script setup lang="ts">
import Button from "primevue/button";
import Card from "primevue/card";
<\/script>

<template>
  <Card class="w-80">
    <template #title>Project Alpha</template>
    <template #subtitle>A next-generation web application.</template>
    <template #content>
      <p class="text-sm text-muted-foreground">
        This project aims to redefine how teams collaborate in real time.
      </p>
    </template>
    <template #footer>
      <Button label="View details" outlined severity="primary" />
    </template>
  </Card>
</template>`;

const BADGE_CODE = `<script setup lang="ts">
import Tag from "primevue/tag";
<\/script>

<template>
  <div class="flex flex-wrap gap-2">
    <Tag severity="primary" value="Primary" />
    <Tag severity="success" value="Success" />
    <Tag severity="info" value="Info" />
    <Tag severity="warn" value="Warning" />
    <Tag severity="danger" value="Danger" />
    <Tag severity="secondary" value="Secondary" />
    <Tag severity="contrast" value="Contrast" />
  </div>
</template>`;

const AVATAR_CODE = `<script setup lang="ts">
import Avatar from "primevue/avatar";
<\/script>

<template>
  <div class="flex items-center gap-4">
    <Avatar image="https://i.pravatar.cc/150?img=1" shape="circle" size="large" />
    <Avatar label="JD" shape="circle" size="large" />
    <Avatar label="SM" shape="circle" />
    <Avatar label="XS" shape="circle" size="small" />
  </div>
</template>`;

const DIVIDER_CODE = `<script setup lang="ts">
import Divider from "primevue/divider";
<\/script>

<template>
  <div class="flex flex-col gap-4">
    <div class="flex flex-col gap-2">
      <p class="text-sm text-muted-foreground">Section one content.</p>
      <Divider />
      <p class="text-sm text-muted-foreground">Section two content.</p>
    </div>
    <div class="flex items-center" style="height: 2rem">
      <span class="text-sm">Left item</span>
      <Divider layout="vertical" />
      <span class="text-sm">Right item</span>
    </div>
  </div>
</template>`;

const SKELETON_CODE = `<script setup lang="ts">
import Skeleton from "primevue/skeleton";
<\/script>

<template>
  <div class="flex items-center gap-4">
    <Skeleton height="3rem" shape="circle" width="3rem" />
    <div class="flex flex-col gap-2">
      <Skeleton height="1rem" width="12rem" />
      <Skeleton height="1rem" width="9rem" />
      <Skeleton height="1rem" width="6rem" />
    </div>
  </div>
</template>`;

const TABLE_CODE = `<script setup lang="ts">
import Column from "primevue/column";
import DataTable from "primevue/datatable";

const rows = [
  { email: "alice@example.com", name: "Alice Martin", role: "Admin" },
  { email: "bob@example.com", name: "Bob Chen", role: "Editor" },
  { email: "carol@example.com", name: "Carol White", role: "Viewer" },
];
<\/script>

<template>
  <DataTable :value="rows" table-style="min-width: 20rem">
    <Column field="name" header="Name" />
    <Column field="email" header="Email" />
    <Column field="role" header="Role" />
  </DataTable>
</template>`;
</script>

<template>
  <div class="flex flex-col gap-16">
    <div>
      <h1 class="text-3xl font-bold tracking-tight text-foreground">Display</h1>
      <p class="mt-2 text-muted-foreground">
        Read-only visual elements for presenting content — cards, badges, avatars, and data tables.
      </p>
    </div>

    <section class="flex flex-col gap-4" id="card">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Card</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          A surface for grouping related content. PrimeVue's
          <code class="rounded bg-muted px-1 py-0.5 text-xs">Card</code> uses named slots:
          <code class="rounded bg-muted px-1 py-0.5 text-xs">#title</code>,
          <code class="rounded bg-muted px-1 py-0.5 text-xs">#subtitle</code>,
          <code class="rounded bg-muted px-1 py-0.5 text-xs">#content</code>, and
          <code class="rounded bg-muted px-1 py-0.5 text-xs">#footer</code>.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <Card class="w-80">
          <template #title>Project Alpha</template>
          <template #subtitle>A next-generation web application.</template>
          <template #content>
            <p class="text-sm text-muted-foreground">
              This project aims to redefine how teams collaborate in real time.
            </p>
          </template>
          <template #footer>
            <Button label="View details" outlined severity="primary" />
          </template>
        </Card>
      </div>
      <CodeBlock
        :code="CARD_CODE"
        collapsible
        :default-open="false"
        filename="Card.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="badge">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Badge</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          A compact label for status or category via
          <code class="rounded bg-muted px-1 py-0.5 text-xs">Tag</code>. Use the
          <code class="rounded bg-muted px-1 py-0.5 text-xs">severity</code> prop: primary, success,
          info, warn, danger, secondary, contrast.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <div class="flex flex-wrap gap-2">
          <Tag severity="primary" value="Primary" />
          <Tag severity="success" value="Success" />
          <Tag severity="info" value="Info" />
          <Tag severity="warn" value="Warning" />
          <Tag severity="danger" value="Danger" />
          <Tag severity="secondary" value="Secondary" />
          <Tag rounded severity="contrast" value="Rounded" />
        </div>
      </div>
      <CodeBlock
        :code="BADGE_CODE"
        collapsible
        :default-open="false"
        filename="Badge.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="avatar">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Avatar</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          A circular or square user image via
          <code class="rounded bg-muted px-1 py-0.5 text-xs">Avatar</code>. Use the
          <code class="rounded bg-muted px-1 py-0.5 text-xs">image</code> prop or
          <code class="rounded bg-muted px-1 py-0.5 text-xs">label</code> for initials fallback.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <div class="flex items-center gap-4">
          <Avatar image="https://i.pravatar.cc/150?img=1" shape="circle" size="large" />
          <Avatar label="JD" shape="circle" size="large" />
          <Avatar label="SM" shape="circle" />
          <Avatar label="XS" shape="circle" size="small" />
        </div>
      </div>
      <CodeBlock
        :code="AVATAR_CODE"
        collapsible
        :default-open="false"
        filename="Avatar.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="separator">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Separator</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          A thin dividing line via
          <code class="rounded bg-muted px-1 py-0.5 text-xs">Divider</code>. Use
          <code class="rounded bg-muted px-1 py-0.5 text-xs">layout="vertical"</code> for inline
          separators.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <div class="flex flex-col gap-6">
          <div class="flex flex-col gap-2">
            <p class="text-sm text-muted-foreground">Section one content.</p>
            <Divider />
            <p class="text-sm text-muted-foreground">Section two content.</p>
          </div>
          <div class="flex items-center" style="height: 2rem">
            <span class="text-sm">Left item</span>
            <Divider layout="vertical" />
            <span class="text-sm">Right item</span>
          </div>
        </div>
      </div>
      <CodeBlock
        :code="DIVIDER_CODE"
        collapsible
        :default-open="false"
        filename="Separator.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="skeleton">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Skeleton</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          Animated placeholder blocks for loading states. Use
          <code class="rounded bg-muted px-1 py-0.5 text-xs">shape="circle"</code> for avatar
          placeholders.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <div class="flex items-center gap-4">
          <Skeleton height="3rem" shape="circle" width="3rem" />
          <div class="flex flex-col gap-2">
            <Skeleton height="1rem" width="12rem" />
            <Skeleton height="1rem" width="9rem" />
            <Skeleton height="1rem" width="6rem" />
          </div>
        </div>
      </div>
      <CodeBlock
        :code="SKELETON_CODE"
        collapsible
        :default-open="false"
        filename="Skeleton.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="table">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Table</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          A structured grid via
          <code class="rounded bg-muted px-1 py-0.5 text-xs">DataTable</code> +
          <code class="rounded bg-muted px-1 py-0.5 text-xs">Column</code>. Bind
          <code class="rounded bg-muted px-1 py-0.5 text-xs">:value</code> to your row array and use
          <code class="rounded bg-muted px-1 py-0.5 text-xs">field</code> /
          <code class="rounded bg-muted px-1 py-0.5 text-xs">header</code> on each column.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <DataTable :value="TABLE_ROWS" table-style="min-width: 20rem">
          <Column field="name" header="Name" />
          <Column field="email" header="Email" />
          <Column field="role" header="Role" />
        </DataTable>
      </div>
      <CodeBlock
        :code="TABLE_CODE"
        collapsible
        :default-open="false"
        filename="Table.vue"
        language="vue"
      />
    </section>
  </div>
</template>
