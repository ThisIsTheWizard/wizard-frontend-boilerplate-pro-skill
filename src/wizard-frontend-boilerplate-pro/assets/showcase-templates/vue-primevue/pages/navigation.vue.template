<script setup lang="ts">
import { ref } from "vue";

import Breadcrumb from "primevue/breadcrumb";
import Menubar from "primevue/menubar";
import Paginator from "primevue/paginator";
import Tab from "primevue/tab";
import TabList from "primevue/tablist";
import TabPanel from "primevue/tabpanel";
import TabPanels from "primevue/tabpanels";
import Tabs from "primevue/tabs";

import CodeBlock from "@/components/CodeBlock.vue";

const activeTab = ref("account");
const paginatorFirst = ref(0);

const BREADCRUMB_HOME = { icon: "pi pi-home", url: "/" };
const BREADCRUMB_ITEMS = [
  { label: "Library", url: "/library" },
  { label: "Navigation", url: "/library/navigation" },
  { label: "Breadcrumb" },
];

const MENU_MODEL = [
  {
    items: [
      { label: "Overview", url: "/products" },
      { label: "Pricing", url: "/products/pricing" },
    ],
    label: "Products",
  },
  {
    items: [
      { label: "Docs", url: "/docs" },
      { label: "Blog", url: "/blog" },
    ],
    label: "Resources",
  },
  { label: "About", url: "/about" },
];

const TABS_CODE = `<script setup lang="ts">
import { ref } from "vue";
import Tab from "primevue/tab";
import TabList from "primevue/tablist";
import TabPanel from "primevue/tabpanel";
import TabPanels from "primevue/tabpanels";
import Tabs from "primevue/tabs";

const activeTab = ref("account");
<\/script>

<template>
  <Tabs v-model:value="activeTab">
    <TabList>
      <Tab value="account">Account</Tab>
      <Tab value="password">Password</Tab>
      <Tab value="notifications">Notifications</Tab>
    </TabList>
    <TabPanels>
      <TabPanel value="account">
        <p class="text-sm text-muted-foreground">Manage your account settings and preferences.</p>
      </TabPanel>
      <TabPanel value="password">
        <p class="text-sm text-muted-foreground">Change your password and security options.</p>
      </TabPanel>
      <TabPanel value="notifications">
        <p class="text-sm text-muted-foreground">Configure how you receive notifications.</p>
      </TabPanel>
    </TabPanels>
  </Tabs>
</template>`;

const BREADCRUMB_CODE = `<script setup lang="ts">
import Breadcrumb from "primevue/breadcrumb";

const home = { icon: "pi pi-home", url: "/" };
const items = [
  { label: "Library", url: "/library" },
  { label: "Navigation" },
];
<\/script>

<template>
  <Breadcrumb :home="home" :model="items" />
</template>`;

const PAGINATION_CODE = `<script setup lang="ts">
import { ref } from "vue";
import Paginator from "primevue/paginator";

const first = ref(0);
<\/script>

<template>
  <Paginator v-model:first="first" :rows="10" :total-records="100" />
</template>`;

const NAV_MENU_CODE = `<script setup lang="ts">
import Menubar from "primevue/menubar";

const model = [
  {
    label: "Products",
    items: [
      { label: "Overview", url: "/products" },
      { label: "Pricing", url: "/products/pricing" },
    ],
  },
  { label: "About", url: "/about" },
];
<\/script>

<template>
  <Menubar :model="model" />
</template>`;
</script>

<template>
  <div class="flex flex-col gap-16">
    <div>
      <h1 class="text-3xl font-bold tracking-tight text-foreground">Navigation</h1>
      <p class="mt-2 text-muted-foreground">
        Components for moving between pages and sections — tabs, breadcrumbs, pagination, and menus.
      </p>
    </div>

    <section class="flex flex-col gap-4" id="tabs">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Tabs</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          A tabbed interface via
          <code class="rounded bg-muted px-1 py-0.5 text-xs">Tabs</code> +
          <code class="rounded bg-muted px-1 py-0.5 text-xs">TabList</code> +
          <code class="rounded bg-muted px-1 py-0.5 text-xs">Tab</code> +
          <code class="rounded bg-muted px-1 py-0.5 text-xs">TabPanels</code> +
          <code class="rounded bg-muted px-1 py-0.5 text-xs">TabPanel</code>. Bind
          <code class="rounded bg-muted px-1 py-0.5 text-xs">v-model:value</code> on the root.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <Tabs v-model:value="activeTab">
          <TabList>
            <Tab value="account">Account</Tab>
            <Tab value="password">Password</Tab>
            <Tab value="notifications">Notifications</Tab>
          </TabList>
          <TabPanels>
            <TabPanel value="account">
              <p class="mt-3 text-sm text-muted-foreground">
                Manage your account settings and preferences.
              </p>
            </TabPanel>
            <TabPanel value="password">
              <p class="mt-3 text-sm text-muted-foreground">
                Change your password and security options.
              </p>
            </TabPanel>
            <TabPanel value="notifications">
              <p class="mt-3 text-sm text-muted-foreground">
                Configure how you receive notifications.
              </p>
            </TabPanel>
          </TabPanels>
        </Tabs>
      </div>
      <CodeBlock
        :code="TABS_CODE"
        collapsible
        :default-open="false"
        filename="Tabs.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="breadcrumb">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Breadcrumb</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          A landmark trail via
          <code class="rounded bg-muted px-1 py-0.5 text-xs">Breadcrumb</code>. Pass a
          <code class="rounded bg-muted px-1 py-0.5 text-xs">:home</code> item and a
          <code class="rounded bg-muted px-1 py-0.5 text-xs">:model</code> array of
          <code class="rounded bg-muted px-1 py-0.5 text-xs">{ label, url }</code> objects.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <Breadcrumb :home="BREADCRUMB_HOME" :model="BREADCRUMB_ITEMS" />
      </div>
      <CodeBlock
        :code="BREADCRUMB_CODE"
        collapsible
        :default-open="false"
        filename="Breadcrumb.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="pagination">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">Pagination</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          Page navigation via
          <code class="rounded bg-muted px-1 py-0.5 text-xs">Paginator</code>. Bind
          <code class="rounded bg-muted px-1 py-0.5 text-xs">v-model:first</code> (byte offset) and
          pass <code class="rounded bg-muted px-1 py-0.5 text-xs">:rows</code> +
          <code class="rounded bg-muted px-1 py-0.5 text-xs">:totalRecords</code>.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <Paginator v-model:first="paginatorFirst" :rows="10" :total-records="100" />
      </div>
      <CodeBlock
        :code="PAGINATION_CODE"
        collapsible
        :default-open="false"
        filename="Pagination.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="navigation-menu">
      <div class="border-l-2 border-primary/40 pl-3">
        <h2 class="text-xl font-semibold text-foreground">NavigationMenu</h2>
        <p class="mt-1 text-sm text-muted-foreground">
          A horizontal menu bar via
          <code class="rounded bg-muted px-1 py-0.5 text-xs">Menubar</code>. Pass a
          <code class="rounded bg-muted px-1 py-0.5 text-xs">:model</code> array where each item
          can have nested
          <code class="rounded bg-muted px-1 py-0.5 text-xs">items</code> for dropdowns.
        </p>
      </div>
      <div class="rounded-lg border border-border bg-muted/50 p-8">
        <Menubar :model="MENU_MODEL" />
      </div>
      <CodeBlock
        :code="NAV_MENU_CODE"
        collapsible
        :default-open="false"
        filename="NavigationMenu.vue"
        language="vue"
      />
    </section>
  </div>
</template>
