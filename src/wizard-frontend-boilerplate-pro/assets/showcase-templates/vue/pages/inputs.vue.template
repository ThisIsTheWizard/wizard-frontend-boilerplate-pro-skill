<script setup lang="ts">
import { ref } from "vue";

import { Button } from "@/components/ui/button";
import { Checkbox } from "@/components/ui/checkbox";
import { CodeBlock } from "@/components/ui/code-block";
import { Input } from "@/components/ui/input";
import { RadioGroup } from "@/components/ui/radio-group";
import { Select } from "@/components/ui/select";
import { Switch } from "@/components/ui/switch";
import { Textarea } from "@/components/ui/textarea";

const BUTTON_CODE = `<script setup lang="ts">
import { Button } from "@/components/ui/button";
<\/script>

<template>
  <div class="flex flex-wrap gap-2">
    <Button>Default</Button>
    <Button variant="secondary">Secondary</Button>
    <Button variant="outline">Outline</Button>
    <Button variant="ghost">Ghost</Button>
    <Button variant="destructive">Destructive</Button>
    <Button variant="link">Link</Button>
  </div>
</template>`;

const INPUT_CODE = `<script setup lang="ts">
import { Input } from "@/components/ui/input";
<\/script>

<template>
  <Input
    helper-text="We'll never share your email."
    id="email"
    label="Email address"
    placeholder="you@example.com"
    type="email"
  />
</template>`;

const TEXTAREA_CODE = `<script setup lang="ts">
import { Textarea } from "@/components/ui/textarea";
<\/script>

<template>
  <Textarea
    helper-text="Describe your project in a few sentences."
    id="bio"
    label="Bio"
    placeholder="Tell us about yourself…"
    :rows="4"
  />
</template>`;

const SELECT_CODE = `<script setup lang="ts">
import { ref } from "vue";
import { Select } from "@/components/ui/select";

const FRAMEWORK_OPTIONS = [
  { label: "Next.js", value: "nextjs" },
  { label: "Remix", value: "remix" },
  { label: "Astro", value: "astro" },
  { label: "SvelteKit", value: "sveltekit" },
];

const framework = ref("");
<\/script>

<template>
  <Select
    label="Framework"
    :options="FRAMEWORK_OPTIONS"
    placeholder="Select a framework…"
    :value="framework"
    @change="val => framework = val"
  />
</template>`;

const CHECKBOX_CODE = `<script setup lang="ts">
import { ref } from "vue";
import { Checkbox } from "@/components/ui/checkbox";

const checked = ref(false);
<\/script>

<template>
  <Checkbox
    :checked="checked"
    id="terms"
    label="Accept terms and conditions"
    @checkedChange="val => checked = val"
  />
</template>`;

const RADIO_GROUP_CODE = `<script setup lang="ts">
import { ref } from "vue";
import { RadioGroup } from "@/components/ui/radio-group";

const PLAN_OPTIONS = [
  { label: "Free", value: "free" },
  { label: "Pro", value: "pro" },
  { label: "Enterprise", value: "enterprise" },
];

const plan = ref("free");
<\/script>

<template>
  <RadioGroup
    label="Plan"
    :options="PLAN_OPTIONS"
    :value="plan"
    @valueChange="val => plan = val"
  />
</template>`;

const SWITCH_CODE = `<script setup lang="ts">
import { ref } from "vue";
import { Switch } from "@/components/ui/switch";

const enabled = ref(false);
<\/script>

<template>
  <Switch
    :checked="enabled"
    id="notifications"
    label="Email notifications"
    @checkedChange="val => enabled = val"
  />
</template>`;

const FRAMEWORK_OPTIONS = [
  { label: "Next.js", value: "nextjs" },
  { label: "Remix", value: "remix" },
  { label: "Astro", value: "astro" },
  { label: "SvelteKit", value: "sveltekit" },
];

const PLAN_OPTIONS = [
  { label: "Free", value: "free" },
  { label: "Pro", value: "pro" },
  { label: "Enterprise", value: "enterprise" },
];

const checkboxChecked = ref(false);
const framework = ref("");
const notifications = ref(false);
const plan = ref("free");
</script>

<template>
  <div class="flex flex-col gap-16">
    <div>
      <h1 class="text-3xl font-bold tracking-tight text-foreground">Inputs</h1>
      <p class="mt-2 text-base leading-relaxed text-muted-foreground">
        Form controls for collecting user input — buttons, text fields, selections, and toggles.
      </p>
    </div>

    <section class="flex flex-col gap-4" id="button">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">Button</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          Triggers an action or event with support for multiple visual variants and sizes.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <div class="flex flex-col gap-4">
          <div class="flex flex-wrap gap-2">
            <Button>Default</Button>
            <Button variant="secondary">Secondary</Button>
            <Button variant="outline">Outline</Button>
            <Button variant="ghost">Ghost</Button>
            <Button variant="destructive">Destructive</Button>
            <Button variant="link">Link</Button>
          </div>
          <div class="flex flex-wrap items-center gap-2">
            <Button size="sm">Small</Button>
            <Button size="default">Default</Button>
            <Button size="lg">Large</Button>
            <Button :disabled="true">Disabled</Button>
            <Button :loading="true">Loading</Button>
          </div>
        </div>
      </div>
      <CodeBlock
        :code="BUTTON_CODE"
        :collapsible="true"
        :default-open="false"
        filename="Button.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="input">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">Input</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          A single-line text field with optional label, helper text, and error state.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <div class="flex max-w-sm flex-col gap-4">
          <Input
            helper-text="We'll never share your email."
            id="email"
            label="Email address"
            placeholder="you@example.com"
            type="email"
          />
          <Input
            default-value="invalid user!"
            error="Please enter a valid username."
            id="username-error"
            label="Username"
          />
          <Input
            :disabled="true"
            id="disabled-input"
            label="Disabled"
            placeholder="Not editable"
          />
        </div>
      </div>
      <CodeBlock
        :code="INPUT_CODE"
        :collapsible="true"
        :default-open="false"
        filename="Input.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="textarea">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">Textarea</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          A multi-line text input with label, helper text, and optional auto-resize.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <div class="flex max-w-sm flex-col gap-4">
          <Textarea
            helper-text="Describe your project in a few sentences."
            id="bio"
            label="Bio"
            placeholder="Tell us about yourself…"
            :rows="4"
          />
          <Textarea
            error="Message is required."
            id="message-error"
            label="Message"
            placeholder="Enter your message…"
          />
        </div>
      </div>
      <CodeBlock
        :code="TEXTAREA_CODE"
        :collapsible="true"
        :default-open="false"
        filename="Textarea.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="select">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">Select</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          A dropdown for picking a single value from a list, with keyboard navigation and ARIA listbox
          semantics.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <div class="flex max-w-sm flex-col gap-4">
          <Select
            label="Framework"
            :options="FRAMEWORK_OPTIONS"
            placeholder="Select a framework…"
            :value="framework"
            @change="val => (framework = val)"
          />
          <Select
            :disabled="true"
            label="Disabled"
            :options="FRAMEWORK_OPTIONS"
            placeholder="Not selectable"
          />
        </div>
      </div>
      <CodeBlock
        :code="SELECT_CODE"
        :collapsible="true"
        :default-open="false"
        filename="Select.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="checkbox">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">Checkbox</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          A binary toggle supporting checked, unchecked, and indeterminate states with full keyboard
          and ARIA support.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <div class="flex flex-col gap-3">
          <Checkbox
            :checked="checkboxChecked"
            id="terms"
            label="Accept terms and conditions"
            @checkedChange="val => (checkboxChecked = val)"
          />
          <Checkbox :default-checked="true" id="checked-demo" label="Checked by default" />
          <Checkbox :indeterminate="true" id="indeterminate-demo" label="Indeterminate state" />
          <Checkbox :disabled="true" id="disabled-checkbox" label="Disabled" />
        </div>
      </div>
      <CodeBlock
        :code="CHECKBOX_CODE"
        :collapsible="true"
        :default-open="false"
        filename="Checkbox.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="radio-group">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">RadioGroup</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          A set of mutually exclusive options with arrow-key navigation within the group.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <div class="flex flex-col gap-6 sm:flex-row sm:gap-16">
          <RadioGroup
            label="Plan (vertical)"
            :options="PLAN_OPTIONS"
            :value="plan"
            @valueChange="val => (plan = val)"
          />
          <RadioGroup
            label="Plan (horizontal)"
            orientation="horizontal"
            :options="PLAN_OPTIONS"
            :value="plan"
            @valueChange="val => (plan = val)"
          />
        </div>
      </div>
      <CodeBlock
        :code="RADIO_GROUP_CODE"
        :collapsible="true"
        :default-open="false"
        filename="RadioGroup.vue"
        language="vue"
      />
    </section>

    <section class="flex flex-col gap-4" id="switch">
      <div class="border-l-[3px] border-primary/60 pl-4">
        <h2 class="text-xl font-semibold text-foreground">Switch</h2>
        <p class="mt-1 text-sm leading-relaxed text-muted-foreground">
          An on/off toggle for binary settings, announced as a switch by assistive technologies.
        </p>
      </div>
      <div class="rounded-xl border border-border/60 bg-muted/30 p-8 shadow-sm">
        <div class="flex flex-col gap-3">
          <Switch
            :checked="notifications"
            id="notifications"
            label="Email notifications"
            @checkedChange="val => (notifications = val)"
          />
          <Switch :default-checked="true" id="marketing" label="Marketing emails" />
          <Switch :disabled="true" id="disabled-switch" label="Disabled" />
        </div>
      </div>
      <CodeBlock
        :code="SWITCH_CODE"
        :collapsible="true"
        :default-open="false"
        filename="Switch.vue"
        language="vue"
      />
    </section>
  </div>
</template>
