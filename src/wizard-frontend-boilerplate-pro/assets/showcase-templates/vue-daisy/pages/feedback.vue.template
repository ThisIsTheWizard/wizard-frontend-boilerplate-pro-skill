<script setup lang="ts">
import { ref } from "vue";

import { CodeBlock } from "@/components/ui/code-block";

type ToastKind = "error" | "info" | "success" | "warning";

const TOAST_MESSAGES: Record<ToastKind, string> = {
  error: "Something went wrong.",
  info: "Event has been created.",
  success: "Profile saved successfully.",
  warning: "Unsaved changes detected.",
};

const toast = ref<ToastKind | null>(null);
const modalOpen = ref(false);
let toastTimer: ReturnType<typeof setTimeout> | null = null;

function showToast(kind: ToastKind) {
  if (toastTimer) clearTimeout(toastTimer);
  toast.value = kind;
  toastTimer = setTimeout(() => {
    toast.value = null;
  }, 3000);
}

const ALERT_CODE = `<template>
  <div class="flex flex-col gap-3">
    <div class="alert alert-info" role="alert">
      <svg class="h-5 w-5 shrink-0" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10" /><path d="M12 16v-4M12 8h.01" /></svg>
      <div>
        <h3 class="font-bold text-sm">Heads up</h3>
        <div class="text-xs">You can add components using the CLI.</div>
      </div>
    </div>
    <div class="alert alert-success" role="alert">
      <svg class="h-5 w-5 shrink-0" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" /><polyline points="22 4 12 14.01 9 11.01" /></svg>
      <div>
        <h3 class="font-bold text-sm">Success</h3>
        <div class="text-xs">Your profile has been saved.</div>
      </div>
    </div>
    <div class="alert alert-warning" role="alert">
      <div><h3 class="font-bold text-sm">Warning</h3><div class="text-xs">Unsaved changes will be lost.</div></div>
    </div>
    <div class="alert alert-error" role="alert">
      <div><h3 class="font-bold text-sm">Error</h3><div class="text-xs">Something went wrong.</div></div>
    </div>
  </div>
</template>`;

const TOAST_CODE = `<script setup lang="ts">
import { ref } from "vue";
type ToastKind = "error" | "info" | "success" | "warning";
const toast = ref<ToastKind | null>(null);
function showToast(kind: ToastKind) {
  toast.value = kind;
  setTimeout(() => { toast.value = null; }, 3000);
}
<\/script>

<template>
  <div class="flex flex-wrap gap-2">
    <button class="btn btn-primary btn-sm" @click="showToast('info')" type="button">Info</button>
    <button class="btn btn-success btn-sm" @click="showToast('success')" type="button">Success</button>
    <button class="btn btn-error btn-sm" @click="showToast('error')" type="button">Error</button>
    <button class="btn btn-warning btn-sm" @click="showToast('warning')" type="button">Warning</button>
  </div>
  <Teleport to="body">
    <div v-if="toast" class="toast toast-end toast-bottom z-50">
      <div :class="\`alert alert-\${toast}\`" role="alert">
        <span class="text-sm">{{ messages[toast] }}</span>
      </div>
    </div>
  </Teleport>
</template>`;

const PROGRESS_CODE = `<template>
  <div class="flex flex-col gap-3">
    <progress class="progress progress-primary w-full" max="100" value="25" />
    <progress class="progress progress-success w-full" max="100" value="60" />
    <progress class="progress progress-warning w-full" max="100" value="80" />
    <progress class="progress progress-error w-full" max="100" value="40" />
  </div>
</template>`;

const TOOLTIP_CODE = `<template>
  <div class="flex flex-wrap gap-4">
    <div class="tooltip tooltip-top" data-tip="Tooltip on top">
      <button class="btn btn-sm btn-outline" type="button">Top</button>
    </div>
    <div class="tooltip tooltip-right" data-tip="Tooltip on right">
      <button class="btn btn-sm btn-outline" type="button">Right</button>
    </div>
    <div class="tooltip tooltip-bottom" data-tip="Tooltip on bottom">
      <button class="btn btn-sm btn-outline" type="button">Bottom</button>
    </div>
    <div class="tooltip tooltip-left" data-tip="Tooltip on left">
      <button class="btn btn-sm btn-outline" type="button">Left</button>
    </div>
  </div>
</template>`;

const MODAL_CODE = `<script setup lang="ts">
import { ref } from "vue";
const modalOpen = ref(false);
<\/script>

<template>
  <button class="btn btn-primary" @click="modalOpen = true" type="button">Open modal</button>
  <dialog :open="modalOpen" class="modal">
    <div class="modal-box">
      <h3 class="font-bold text-lg">Confirm action</h3>
      <p class="py-4 text-base-content/70 text-sm">
        This action cannot be undone. Are you sure you want to proceed?
      </p>
      <div class="modal-action">
        <button class="btn btn-ghost" @click="modalOpen = false" type="button">Cancel</button>
        <button class="btn btn-primary" @click="modalOpen = false" type="button">Confirm</button>
      </div>
    </div>
    <div class="modal-backdrop" @click="modalOpen = false" />
  </dialog>
</template>`;
</script>

<template>
  <div class="flex flex-col gap-4">
    <div>
      <h1 class="text-xl font-semibold mb-1">Feedback</h1>
      <p class="text-base-content/60 text-sm">
        Status indicators, notifications, and modal interactions.
      </p>
    </div>

    <!-- Alert -->
    <section id="alert" class="mb-2">
      <div class="mb-2 pl-3" style="border-left: 3px solid var(--color-primary)">
        <h5 class="font-semibold mb-1">Alert</h5>
        <p class="text-base-content/60 text-sm mb-0">
          Inline banners using <code class="kbd kbd-sm">alert</code> with
          <code class="kbd kbd-sm">alert-info</code>,
          <code class="kbd kbd-sm">alert-success</code>,
          <code class="kbd kbd-sm">alert-warning</code>, and
          <code class="kbd kbd-sm">alert-error</code>.
        </p>
      </div>
      <div class="bg-base-200 rounded-box p-4 mb-3">
        <div class="flex flex-col gap-3">
          <div class="alert alert-info" role="alert">
            <svg class="h-5 w-5 shrink-0" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10" /><path d="M12 16v-4M12 8h.01" /></svg>
            <div><h3 class="font-bold text-sm">Heads up</h3><div class="text-xs">You can add components using the CLI.</div></div>
          </div>
          <div class="alert alert-success" role="alert">
            <svg class="h-5 w-5 shrink-0" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" /><polyline points="22 4 12 14.01 9 11.01" /></svg>
            <div><h3 class="font-bold text-sm">Success</h3><div class="text-xs">Your profile has been saved.</div></div>
          </div>
          <div class="alert alert-warning" role="alert">
            <div><h3 class="font-bold text-sm">Warning</h3><div class="text-xs">Unsaved changes will be lost.</div></div>
          </div>
          <div class="alert alert-error" role="alert">
            <div><h3 class="font-bold text-sm">Error</h3><div class="text-xs">Something went wrong. Please try again.</div></div>
          </div>
        </div>
      </div>
      <CodeBlock :code="ALERT_CODE" collapsible :default-open="false" filename="Alert.vue" language="vue" />
    </section>

    <div class="divider my-2" />

    <!-- Toast -->
    <section id="toast" class="mb-2">
      <div class="mb-2 pl-3" style="border-left: 3px solid var(--color-primary)">
        <h5 class="font-semibold mb-1">Toast</h5>
        <p class="text-base-content/60 text-sm mb-0">
          Temporary notifications using <code class="kbd kbd-sm">toast toast-end toast-bottom</code>
          as a fixed container, auto-dismissed with <code class="kbd kbd-sm">setTimeout</code>.
          Use <code class="kbd kbd-sm">Teleport</code> to render outside the layout.
        </p>
      </div>
      <div class="bg-base-200 rounded-box p-4 mb-3">
        <div class="flex flex-wrap gap-2">
          <button class="btn btn-primary btn-sm" type="button" @click="showToast('info')">Info</button>
          <button class="btn btn-success btn-sm" type="button" @click="showToast('success')">Success</button>
          <button class="btn btn-error btn-sm" type="button" @click="showToast('error')">Error</button>
          <button class="btn btn-warning btn-sm" type="button" @click="showToast('warning')">Warning</button>
        </div>
      </div>
      <CodeBlock :code="TOAST_CODE" collapsible :default-open="false" filename="Toast.vue" language="vue" />
    </section>

    <!-- Toast portal -->
    <Teleport to="body">
      <div v-if="toast" class="toast toast-end toast-bottom z-50">
        <div :class="`alert alert-${toast}`" role="alert">
          <span class="text-sm">{{ TOAST_MESSAGES[toast] }}</span>
        </div>
      </div>
    </Teleport>

    <div class="divider my-2" />

    <!-- Progress -->
    <section id="progress" class="mb-2">
      <div class="mb-2 pl-3" style="border-left: 3px solid var(--color-primary)">
        <h5 class="font-semibold mb-1">Progress</h5>
        <p class="text-base-content/60 text-sm mb-0">
          Native <code class="kbd kbd-sm">&lt;progress&gt;</code> styled with
          <code class="kbd kbd-sm">progress progress-primary</code>.
          Change variant (<code class="kbd kbd-sm">progress-success</code>,
          <code class="kbd kbd-sm">progress-warning</code>,
          <code class="kbd kbd-sm">progress-error</code>) and set <code class="kbd kbd-sm">value</code> / <code class="kbd kbd-sm">max</code>.
        </p>
      </div>
      <div class="bg-base-200 rounded-box p-4 mb-3">
        <div class="flex flex-col gap-3">
          <progress class="progress progress-primary w-full" max="100" value="25" />
          <progress class="progress progress-success w-full" max="100" value="60" />
          <progress class="progress progress-warning w-full" max="100" value="80" />
          <progress class="progress progress-error w-full" max="100" value="40" />
        </div>
      </div>
      <CodeBlock :code="PROGRESS_CODE" collapsible :default-open="false" filename="Progress.vue" language="vue" />
    </section>

    <div class="divider my-2" />

    <!-- Tooltip -->
    <section id="tooltip" class="mb-2">
      <div class="mb-2 pl-3" style="border-left: 3px solid var(--color-primary)">
        <h5 class="font-semibold mb-1">Tooltip</h5>
        <p class="text-base-content/60 text-sm mb-0">
          Pure-CSS tooltip via <code class="kbd kbd-sm">tooltip</code> wrapper and
          <code class="kbd kbd-sm">data-tip</code>. Direction modifiers:
          <code class="kbd kbd-sm">tooltip-top/right/bottom/left</code>.
        </p>
      </div>
      <div class="bg-base-200 rounded-box p-4 mb-3">
        <div class="flex flex-wrap gap-4">
          <div class="tooltip tooltip-top" data-tip="Tooltip on top">
            <button class="btn btn-sm btn-outline" type="button">Top</button>
          </div>
          <div class="tooltip tooltip-right" data-tip="Tooltip on right">
            <button class="btn btn-sm btn-outline" type="button">Right</button>
          </div>
          <div class="tooltip tooltip-bottom" data-tip="Tooltip on bottom">
            <button class="btn btn-sm btn-outline" type="button">Bottom</button>
          </div>
          <div class="tooltip tooltip-left" data-tip="Tooltip on left">
            <button class="btn btn-sm btn-outline" type="button">Left</button>
          </div>
        </div>
      </div>
      <CodeBlock :code="TOOLTIP_CODE" collapsible :default-open="false" filename="Tooltip.vue" language="vue" />
    </section>

    <div class="divider my-2" />

    <!-- Modal -->
    <section id="modal" class="mb-2">
      <div class="mb-2 pl-3" style="border-left: 3px solid var(--color-primary)">
        <h5 class="font-semibold mb-1">Modal</h5>
        <p class="text-base-content/60 text-sm mb-0">
          A dialog using the <code class="kbd kbd-sm">modal</code> class on a native
          <code class="kbd kbd-sm">&lt;dialog&gt;</code> element controlled with
          <code class="kbd kbd-sm">:open="modalOpen"</code>. Use
          <code class="kbd kbd-sm">modal-box</code>, <code class="kbd kbd-sm">modal-action</code>,
          and <code class="kbd kbd-sm">modal-backdrop</code> for structure.
        </p>
      </div>
      <div class="bg-base-200 rounded-box p-4 mb-3">
        <button class="btn btn-primary" type="button" @click="modalOpen = true">Open modal</button>
      </div>
      <CodeBlock :code="MODAL_CODE" collapsible :default-open="false" filename="Modal.vue" language="vue" />
    </section>

    <!-- Modal dialog -->
    <dialog :open="modalOpen" class="modal">
      <div class="modal-box">
        <h3 class="font-bold text-lg">Confirm action</h3>
        <p class="py-4 text-base-content/70 text-sm">
          This action cannot be undone. Are you sure you want to proceed?
        </p>
        <div class="modal-action">
          <button class="btn btn-ghost" type="button" @click="modalOpen = false">Cancel</button>
          <button class="btn btn-primary" type="button" @click="modalOpen = false">Confirm</button>
        </div>
      </div>
      <div class="modal-backdrop" @click="modalOpen = false" />
    </dialog>
  </div>
</template>
