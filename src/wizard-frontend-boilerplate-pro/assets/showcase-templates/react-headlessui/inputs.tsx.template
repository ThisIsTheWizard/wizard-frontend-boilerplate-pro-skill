"use client";

import { useState } from "react";
import {
  Checkbox,
  Field,
  Input,
  Label,
  Listbox,
  ListboxButton,
  ListboxOption,
  ListboxOptions,
  Radio,
  RadioGroup,
  Switch,
  Textarea,
} from "@headlessui/react";

import { CodeBlock } from "@/components/ui/code-block";
import { cn } from "@/lib/utils";

const BUTTON_CODE = `export function Example() {
  return (
    <div className="flex flex-wrap gap-2">
      <button className="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition hover:opacity-90">
        Primary
      </button>
      <button className="rounded-md border border-border bg-background px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent">
        Secondary
      </button>
      <button className="rounded-md border border-border px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent">
        Outline
      </button>
      <button className="rounded-md px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent">
        Ghost
      </button>
      <button className="rounded-md bg-destructive px-4 py-2 text-sm font-medium text-white transition hover:opacity-90">
        Destructive
      </button>
    </div>
  );
}`;

const INPUT_CODE = `import { Field, Input, Label } from "@headlessui/react";

export function Example() {
  return (
    <Field className="flex flex-col gap-1.5">
      <Label className="text-sm font-medium text-foreground">Email address</Label>
      <Input
        className="rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-ring"
        placeholder="you@example.com"
        type="email"
      />
      <p className="text-sm text-muted-foreground">We'll never share your email.</p>
    </Field>
  );
}`;

const TEXTAREA_CODE = `import { Field, Label, Textarea } from "@headlessui/react";

export function Example() {
  return (
    <Field className="flex flex-col gap-1.5">
      <Label className="text-sm font-medium text-foreground">Bio</Label>
      <Textarea
        className="rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-ring"
        placeholder="Tell us about yourself…"
        rows={4}
      />
      <p className="text-sm text-muted-foreground">Describe your project in a few sentences.</p>
    </Field>
  );
}`;

const SELECT_CODE = `import { Listbox, ListboxButton, ListboxOption, ListboxOptions } from "@headlessui/react";
import { useState } from "react";

const options = ["Next.js", "Remix", "Astro", "SvelteKit"];

export function Example() {
  const [selected, setSelected] = useState(options[0]);

  return (
    <Listbox value={selected} onChange={setSelected}>
      <ListboxButton className="relative w-full rounded-md border border-border bg-background px-3 py-2 text-left text-sm focus:outline-none focus:ring-2 focus:ring-ring">
        {selected}
        <span className="pointer-events-none absolute inset-y-0 right-3 flex items-center text-muted-foreground">▾</span>
      </ListboxButton>
      <ListboxOptions
        anchor="bottom"
        className="z-50 w-[var(--button-width)] rounded-md border border-border bg-surface shadow-lg [--anchor-gap:4px] focus:outline-none"
      >
        {options.map((opt) => (
          <ListboxOption
            key={opt}
            value={opt}
            className="cursor-pointer px-3 py-2 text-sm text-foreground data-[focus]:bg-accent"
          >
            {opt}
          </ListboxOption>
        ))}
      </ListboxOptions>
    </Listbox>
  );
}`;

const CHECKBOX_CODE = `import { Checkbox } from "@headlessui/react";
import { useState } from "react";

export function Example() {
  const [checked, setChecked] = useState(false);

  return (
    <label className="flex cursor-pointer items-center gap-2">
      <Checkbox
        checked={checked}
        onChange={setChecked}
        className="flex h-4 w-4 items-center justify-center rounded border border-border bg-background data-[checked]:border-primary data-[checked]:bg-primary"
      >
        <svg
          className="hidden h-3 w-3 stroke-white data-[checked]:block"
          viewBox="0 0 12 12"
          fill="none"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
        >
          <path d="M2 6l3 3 5-5" />
        </svg>
      </Checkbox>
      <span className="text-sm text-foreground">Accept terms and conditions</span>
    </label>
  );
}`;

const RADIO_GROUP_CODE = `import { Radio, RadioGroup } from "@headlessui/react";
import { useState } from "react";

const plans = ["Free", "Pro", "Enterprise"];

export function Example() {
  const [plan, setPlan] = useState("Free");

  return (
    <RadioGroup value={plan} onChange={setPlan} className="flex flex-col gap-2">
      {plans.map((p) => (
        <Radio key={p} value={p} className="group flex cursor-pointer items-center gap-2">
          <span className="flex h-4 w-4 items-center justify-center rounded-full border border-border group-data-[checked]:border-primary">
            <span className="hidden h-2 w-2 rounded-full bg-primary group-data-[checked]:block" />
          </span>
          <span className="text-sm text-foreground">{p}</span>
        </Radio>
      ))}
    </RadioGroup>
  );
}`;

const SWITCH_CODE = `import { Switch } from "@headlessui/react";
import { useState } from "react";

export function Example() {
  const [enabled, setEnabled] = useState(false);

  return (
    <label className="flex cursor-pointer items-center gap-2">
      <Switch
        checked={enabled}
        onChange={setEnabled}
        className="relative inline-flex h-6 w-11 items-center rounded-full bg-muted-foreground/30 transition data-[checked]:bg-primary"
      >
        <span className="inline-block h-4 w-4 translate-x-1 rounded-full bg-white transition data-[checked]:translate-x-6" />
      </Switch>
      <span className="text-sm text-foreground">Email notifications</span>
    </label>
  );
}`;

const FRAMEWORKS = ["Next.js", "Remix", "Astro", "SvelteKit"];
const PLANS = ["Free", "Pro", "Enterprise"];

export default function InputsPage() {
  const [selected, setSelected] = useState(FRAMEWORKS[0]);
  const [plan, setPlan] = useState("Free");
  const [termsChecked, setTermsChecked] = useState(false);
  const [defaultChecked, setDefaultChecked] = useState(true);
  const [notifications, setNotifications] = useState(false);
  const [marketing, setMarketing] = useState(true);

  return (
    <div className="flex flex-col gap-16">
      <div>
        <h1 className="text-3xl font-bold tracking-tight text-foreground">
          Inputs
        </h1>
        <p className="mt-2 text-muted-foreground">
          Form controls built with Headless UI primitives and Tailwind — fully
          accessible with zero pre-built styles.
        </p>
      </div>

      <section className="flex flex-col gap-4" id="button">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Button</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            Plain HTML buttons styled with Tailwind — Headless UI has no Button
            primitive; use the native element directly.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex flex-col gap-4">
            <div className="flex flex-wrap gap-2">
              <button className="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition hover:opacity-90">
                Primary
              </button>
              <button className="rounded-md border border-border bg-background px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent">
                Secondary
              </button>
              <button className="rounded-md border border-border px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent">
                Outline
              </button>
              <button className="rounded-md px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent">
                Ghost
              </button>
              <button className="rounded-md bg-destructive px-4 py-2 text-sm font-medium text-white transition hover:opacity-90">
                Destructive
              </button>
            </div>
            <div className="flex flex-wrap items-center gap-2">
              <button className="rounded-md bg-primary px-3 py-1.5 text-xs font-medium text-primary-foreground transition hover:opacity-90">
                Small
              </button>
              <button className="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition hover:opacity-90">
                Default
              </button>
              <button className="rounded-md bg-primary px-5 py-2.5 text-base font-medium text-primary-foreground transition hover:opacity-90">
                Large
              </button>
              <button
                disabled
                className="cursor-not-allowed rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground opacity-50"
              >
                Disabled
              </button>
            </div>
          </div>
        </div>
        <CodeBlock
          code={BUTTON_CODE}
          collapsible
          defaultOpen={false}
          filename="Button.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="input">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Input</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            Headless UI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Field</code>{" "}
            +{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Input</code>{" "}
            primitives with{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Label</code>{" "}
            for accessible pairing.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex max-w-sm flex-col gap-4">
            <Field className="flex flex-col gap-1.5">
              <Label className="text-sm font-medium text-foreground">
                Email address
              </Label>
              <Input
                className="rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-ring"
                placeholder="you@example.com"
                type="email"
              />
              <p className="text-sm text-muted-foreground">
                We'll never share your email.
              </p>
            </Field>
            <Field className="flex flex-col gap-1.5">
              <Label className="text-sm font-medium text-foreground">
                Username
              </Label>
              <Input
                aria-invalid
                className="rounded-md border border-destructive bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-destructive"
                defaultValue="invalid user!"
              />
              <p className="text-sm text-destructive">
                Please enter a valid username.
              </p>
            </Field>
            <Field className="flex flex-col gap-1.5">
              <Label className="text-sm font-medium text-foreground opacity-50">
                Disabled
              </Label>
              <Input
                disabled
                className="cursor-not-allowed rounded-md border border-border bg-muted px-3 py-2 text-sm opacity-50 outline-none"
                placeholder="Not editable"
              />
            </Field>
          </div>
        </div>
        <CodeBlock
          code={INPUT_CODE}
          collapsible
          defaultOpen={false}
          filename="Input.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="textarea">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Textarea</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            Headless UI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">
              Textarea
            </code>{" "}
            primitive for multi-line text input.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex max-w-sm flex-col gap-4">
            <Field className="flex flex-col gap-1.5">
              <Label className="text-sm font-medium text-foreground">Bio</Label>
              <Textarea
                className="rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-ring"
                placeholder="Tell us about yourself…"
                rows={4}
              />
              <p className="text-sm text-muted-foreground">
                Describe your project in a few sentences.
              </p>
            </Field>
            <Field className="flex flex-col gap-1.5">
              <Label className="text-sm font-medium text-foreground">
                Message
              </Label>
              <Textarea
                aria-invalid
                className="rounded-md border border-destructive bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-destructive"
                placeholder="Enter your message…"
              />
              <p className="text-sm text-destructive">Message is required.</p>
            </Field>
          </div>
        </div>
        <CodeBlock
          code={TEXTAREA_CODE}
          collapsible
          defaultOpen={false}
          filename="Textarea.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="select">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Select</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            Headless UI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">
              Listbox
            </code>{" "}
            — a fully accessible custom select with keyboard navigation and
            ARIA.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex max-w-sm flex-col gap-4">
            <div className="flex flex-col gap-1.5">
              <label className="text-sm font-medium text-foreground">
                Framework
              </label>
              <Listbox value={selected} onChange={setSelected}>
                <ListboxButton className="relative w-full rounded-md border border-border bg-background px-3 py-2 text-left text-sm focus:outline-none focus:ring-2 focus:ring-ring">
                  {selected}
                  <span className="pointer-events-none absolute inset-y-0 right-3 flex items-center text-muted-foreground">
                    ▾
                  </span>
                </ListboxButton>
                <ListboxOptions
                  anchor="bottom"
                  className="z-50 w-[var(--button-width)] rounded-md border border-border bg-surface shadow-lg [--anchor-gap:4px] focus:outline-none"
                >
                  {FRAMEWORKS.map((opt) => (
                    <ListboxOption
                      key={opt}
                      value={opt}
                      className={cn(
                        "cursor-pointer px-3 py-2 text-sm text-foreground data-[focus]:bg-accent",
                        selected === opt && "font-medium text-primary",
                      )}
                    >
                      {opt}
                    </ListboxOption>
                  ))}
                </ListboxOptions>
              </Listbox>
            </div>
          </div>
        </div>
        <CodeBlock
          code={SELECT_CODE}
          collapsible
          defaultOpen={false}
          filename="Select.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="checkbox">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Checkbox</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            Headless UI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">
              Checkbox
            </code>{" "}
            — styled via{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">
              data-[checked]
            </code>{" "}
            modifier.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex flex-col gap-3">
            <label className="flex cursor-pointer items-center gap-2">
              <Checkbox
                checked={termsChecked}
                onChange={setTermsChecked}
                className="flex h-4 w-4 items-center justify-center rounded border border-border bg-background data-[checked]:border-primary data-[checked]:bg-primary"
              >
                <svg
                  className="hidden h-3 w-3 stroke-white data-[checked]:block"
                  viewBox="0 0 12 12"
                  fill="none"
                  strokeWidth="2"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                >
                  <path d="M2 6l3 3 5-5" />
                </svg>
              </Checkbox>
              <span className="text-sm text-foreground">
                Accept terms and conditions
              </span>
            </label>
            <label className="flex cursor-pointer items-center gap-2">
              <Checkbox
                checked={defaultChecked}
                onChange={setDefaultChecked}
                className="flex h-4 w-4 items-center justify-center rounded border border-border bg-background data-[checked]:border-primary data-[checked]:bg-primary"
              >
                <svg
                  className="hidden h-3 w-3 stroke-white data-[checked]:block"
                  viewBox="0 0 12 12"
                  fill="none"
                  strokeWidth="2"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                >
                  <path d="M2 6l3 3 5-5" />
                </svg>
              </Checkbox>
              <span className="text-sm text-foreground">
                Checked by default
              </span>
            </label>
            <label className="flex cursor-not-allowed items-center gap-2 opacity-50">
              <Checkbox
                disabled
                className="flex h-4 w-4 items-center justify-center rounded border border-border bg-background"
              />
              <span className="text-sm text-foreground">Disabled</span>
            </label>
          </div>
        </div>
        <CodeBlock
          code={CHECKBOX_CODE}
          collapsible
          defaultOpen={false}
          filename="Checkbox.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="radio-group">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">RadioGroup</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            Headless UI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">
              RadioGroup
            </code>{" "}
            +{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Radio</code>{" "}
            with arrow-key navigation and{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">
              group-data-[checked]
            </code>{" "}
            styling.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <RadioGroup
            value={plan}
            onChange={setPlan}
            className="flex flex-col gap-2"
          >
            {PLANS.map((p) => (
              <Radio
                key={p}
                value={p}
                className="group flex cursor-pointer items-center gap-2"
              >
                <span className="flex h-4 w-4 items-center justify-center rounded-full border border-border group-data-[checked]:border-primary">
                  <span className="hidden h-2 w-2 rounded-full bg-primary group-data-[checked]:block" />
                </span>
                <span className="text-sm text-foreground">{p}</span>
              </Radio>
            ))}
          </RadioGroup>
        </div>
        <CodeBlock
          code={RADIO_GROUP_CODE}
          collapsible
          defaultOpen={false}
          filename="RadioGroup.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="switch">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Switch</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            Headless UI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Switch</code>{" "}
            — toggle styled via{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">
              data-[checked]
            </code>{" "}
            and{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">
              translate-x
            </code>
            .
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex flex-col gap-3">
            <label className="flex cursor-pointer items-center gap-2">
              <Switch
                checked={notifications}
                onChange={setNotifications}
                className="relative inline-flex h-6 w-11 items-center rounded-full bg-muted-foreground/30 transition data-[checked]:bg-primary"
              >
                <span className="inline-block h-4 w-4 translate-x-1 rounded-full bg-white transition data-[checked]:translate-x-6" />
              </Switch>
              <span className="text-sm text-foreground">
                Email notifications
              </span>
            </label>
            <label className="flex cursor-pointer items-center gap-2">
              <Switch
                checked={marketing}
                onChange={setMarketing}
                className="relative inline-flex h-6 w-11 items-center rounded-full bg-muted-foreground/30 transition data-[checked]:bg-primary"
              >
                <span className="inline-block h-4 w-4 translate-x-1 rounded-full bg-white transition data-[checked]:translate-x-6" />
              </Switch>
              <span className="text-sm text-foreground">Marketing emails</span>
            </label>
            <label className="flex cursor-not-allowed items-center gap-2 opacity-50">
              <Switch
                disabled
                checked={false}
                onChange={() => {}}
                className="relative inline-flex h-6 w-11 items-center rounded-full bg-muted-foreground/30"
              >
                <span className="inline-block h-4 w-4 translate-x-1 rounded-full bg-white" />
              </Switch>
              <span className="text-sm text-foreground">Disabled</span>
            </label>
          </div>
        </div>
        <CodeBlock
          code={SWITCH_CODE}
          collapsible
          defaultOpen={false}
          filename="Switch.tsx"
          language="tsx"
        />
      </section>
    </div>
  );
}
