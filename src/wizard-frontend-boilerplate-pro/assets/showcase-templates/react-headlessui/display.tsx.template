"use client";

import { CodeBlock } from "@/components/ui/code-block";

const CARD_CODE = `export function Example() {
  return (
    <div className="w-80 rounded-xl border border-border bg-surface p-6 shadow-sm">
      <div className="mb-1 text-lg font-semibold text-foreground">Project Alpha</div>
      <div className="mb-4 text-sm text-muted-foreground">A next-generation web application.</div>
      <p className="text-sm text-muted-foreground">
        This project aims to redefine how teams collaborate in real time.
      </p>
      <div className="mt-6">
        <button className="rounded-md border border-border px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent">
          View details
        </button>
      </div>
    </div>
  );
}`;

const BADGE_CODE = `export function Example() {
  return (
    <div className="flex flex-wrap gap-2">
      <span className="inline-flex items-center rounded-full bg-primary px-2.5 py-0.5 text-xs font-medium text-primary-foreground">
        Default
      </span>
      <span className="inline-flex items-center rounded-full bg-muted px-2.5 py-0.5 text-xs font-medium text-muted-foreground">
        Secondary
      </span>
      <span className="inline-flex items-center rounded-full bg-destructive px-2.5 py-0.5 text-xs font-medium text-white">
        Destructive
      </span>
      <span className="inline-flex items-center rounded-full border border-border px-2.5 py-0.5 text-xs font-medium text-foreground">
        Outline
      </span>
    </div>
  );
}`;

const AVATAR_CODE = `export function Example() {
  return (
    <div className="flex items-center gap-4">
      <div className="flex h-10 w-10 items-center justify-center overflow-hidden rounded-full bg-muted">
        <img alt="Jane Doe" src="https://i.pravatar.cc/150?img=1" className="h-full w-full object-cover" />
      </div>
      <div className="flex h-10 w-10 items-center justify-center rounded-full bg-primary/20 text-sm font-medium text-primary">
        SM
      </div>
      <div className="flex h-8 w-8 items-center justify-center rounded-full bg-primary/20 text-xs font-medium text-primary">
        XS
      </div>
      <div className="flex h-14 w-14 items-center justify-center rounded-full bg-primary/20 text-lg font-medium text-primary">
        LG
      </div>
    </div>
  );
}`;

const SEPARATOR_CODE = `export function Example() {
  return (
    <div className="flex flex-col gap-4">
      <div className="flex flex-col gap-2">
        <p className="text-sm text-muted-foreground">Section one content.</p>
        <hr className="border-border" />
        <p className="text-sm text-muted-foreground">Section two content.</p>
      </div>
      <div className="flex items-center gap-4">
        <span className="text-sm">Left item</span>
        <div className="h-4 w-px bg-border" />
        <span className="text-sm">Right item</span>
      </div>
    </div>
  );
}`;

const SKELETON_CODE = `export function Example() {
  return (
    <div className="flex items-center gap-4">
      <div className="h-12 w-12 animate-pulse rounded-full bg-muted" />
      <div className="flex flex-col gap-2">
        <div className="h-4 w-48 animate-pulse rounded bg-muted" />
        <div className="h-4 w-36 animate-pulse rounded bg-muted" />
        <div className="h-4 w-24 animate-pulse rounded bg-muted" />
      </div>
    </div>
  );
}`;

const TABLE_CODE = `export function Example() {
  return (
    <table className="w-full text-sm">
      <caption className="mb-2 text-sm text-muted-foreground">Team members list</caption>
      <thead>
        <tr className="border-b border-border">
          <th scope="col" className="py-3 pr-4 text-left font-medium text-muted-foreground">Name</th>
          <th scope="col" className="py-3 pr-4 text-left font-medium text-muted-foreground">Email</th>
          <th scope="col" className="py-3 text-left font-medium text-muted-foreground">Role</th>
        </tr>
      </thead>
      <tbody>
        <tr className="border-b border-border">
          <td className="py-3 pr-4 text-foreground">Alice Martin</td>
          <td className="py-3 pr-4 text-foreground">alice@example.com</td>
          <td className="py-3 text-foreground">Admin</td>
        </tr>
      </tbody>
    </table>
  );
}`;

const TEAM = [
  { name: "Alice Martin", email: "alice@example.com", role: "Admin" },
  { name: "Bob Chen", email: "bob@example.com", role: "Editor" },
  { name: "Carol White", email: "carol@example.com", role: "Viewer" },
];

export default function DisplayPage() {
  return (
    <div className="flex flex-col gap-16">
      <div>
        <h1 className="text-3xl font-bold tracking-tight text-foreground">
          Display
        </h1>
        <p className="mt-2 text-muted-foreground">
          Read-only visual elements styled with Tailwind — Headless UI provides
          no display primitives, so these are plain HTML with token-based
          classes.
        </p>
      </div>

      <section className="flex flex-col gap-4" id="card">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Card</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            A surface for grouping related content with an optional header,
            body, and footer — pure Tailwind implementation.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="w-80 rounded-xl border border-border bg-surface p-6 shadow-sm">
            <div className="mb-1 text-lg font-semibold text-foreground">
              Project Alpha
            </div>
            <div className="mb-4 text-sm text-muted-foreground">
              A next-generation web application.
            </div>
            <p className="text-sm text-muted-foreground">
              This project aims to redefine how teams collaborate in real time.
            </p>
            <div className="mt-6">
              <button className="rounded-md border border-border px-4 py-2 text-sm font-medium text-foreground transition hover:bg-accent">
                View details
              </button>
            </div>
          </div>
        </div>
        <CodeBlock
          code={CARD_CODE}
          collapsible
          defaultOpen={false}
          filename="Card.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="badge">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Badge</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            A compact label for status or category — plain{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">span</code>{" "}
            with Tailwind variants.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex flex-wrap gap-2">
            <span className="inline-flex items-center rounded-full bg-primary px-2.5 py-0.5 text-xs font-medium text-primary-foreground">
              Default
            </span>
            <span className="inline-flex items-center rounded-full bg-muted px-2.5 py-0.5 text-xs font-medium text-muted-foreground">
              Secondary
            </span>
            <span className="inline-flex items-center rounded-full bg-destructive px-2.5 py-0.5 text-xs font-medium text-white">
              Destructive
            </span>
            <span className="inline-flex items-center rounded-full border border-border px-2.5 py-0.5 text-xs font-medium text-foreground">
              Outline
            </span>
          </div>
        </div>
        <CodeBlock
          code={BADGE_CODE}
          collapsible
          defaultOpen={false}
          filename="Badge.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="avatar">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Avatar</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            A circular user representation with image or initials fallback —
            plain Tailwind implementation.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex items-center gap-4">
            <div className="flex h-10 w-10 items-center justify-center overflow-hidden rounded-full bg-muted">
              <img
                alt="Jane Doe"
                className="h-full w-full object-cover"
                src="https://i.pravatar.cc/150?img=1"
              />
            </div>
            <div className="flex h-10 w-10 items-center justify-center rounded-full bg-primary/20 text-sm font-medium text-primary">
              SM
            </div>
            <div className="flex h-8 w-8 items-center justify-center rounded-full bg-primary/20 text-xs font-medium text-primary">
              XS
            </div>
            <div className="flex h-14 w-14 items-center justify-center rounded-full bg-primary/20 text-lg font-medium text-primary">
              LG
            </div>
          </div>
        </div>
        <CodeBlock
          code={AVATAR_CODE}
          collapsible
          defaultOpen={false}
          filename="Avatar.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="separator">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Separator</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            A thin dividing line in horizontal or vertical orientation — plain{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">hr</code>{" "}
            or{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">div</code>{" "}
            with Tailwind.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex flex-col gap-6">
            <div className="flex flex-col gap-2">
              <p className="text-sm text-muted-foreground">
                Section one content.
              </p>
              <hr className="border-border" />
              <p className="text-sm text-muted-foreground">
                Section two content.
              </p>
            </div>
            <div className="flex items-center gap-4">
              <span className="text-sm">Left item</span>
              <div className="h-4 w-px bg-border" />
              <span className="text-sm">Right item</span>
            </div>
          </div>
        </div>
        <CodeBlock
          code={SEPARATOR_CODE}
          collapsible
          defaultOpen={false}
          filename="Separator.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="skeleton">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Skeleton</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            Animated placeholder blocks for loading states using{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">
              animate-pulse
            </code>{" "}
            and{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">bg-muted</code>
            .
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex flex-col gap-6">
            <div className="flex items-center gap-4">
              <div className="h-12 w-12 animate-pulse rounded-full bg-muted" />
              <div className="flex flex-col gap-2">
                <div className="h-4 w-48 animate-pulse rounded bg-muted" />
                <div className="h-4 w-36 animate-pulse rounded bg-muted" />
                <div className="h-4 w-24 animate-pulse rounded bg-muted" />
              </div>
            </div>
            <div className="flex flex-col gap-2">
              <div className="h-6 w-32 animate-pulse rounded bg-muted" />
              <div className="h-4 w-full animate-pulse rounded bg-muted" />
              <div className="h-4 w-full animate-pulse rounded bg-muted" />
              <div className="h-4 w-2/3 animate-pulse rounded bg-muted" />
            </div>
          </div>
        </div>
        <CodeBlock
          code={SKELETON_CODE}
          collapsible
          defaultOpen={false}
          filename="Skeleton.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="table">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Table</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            A structured grid for tabular data — semantic HTML table with
            Tailwind token classes.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="overflow-x-auto">
            <table className="w-full text-sm">
              <caption className="mb-2 text-sm text-muted-foreground">
                Team members list
              </caption>
              <thead>
                <tr className="border-b border-border">
                  <th
                    className="py-3 pr-4 text-left font-medium text-muted-foreground"
                    scope="col"
                  >
                    Name
                  </th>
                  <th
                    className="py-3 pr-4 text-left font-medium text-muted-foreground"
                    scope="col"
                  >
                    Email
                  </th>
                  <th
                    className="py-3 text-left font-medium text-muted-foreground"
                    scope="col"
                  >
                    Role
                  </th>
                </tr>
              </thead>
              <tbody>
                {TEAM.map(({ name, email, role }) => (
                  <tr className="border-b border-border last:border-0" key={email}>
                    <td className="py-3 pr-4 text-foreground">{name}</td>
                    <td className="py-3 pr-4 text-foreground">{email}</td>
                    <td className="py-3 text-foreground">{role}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>
        <CodeBlock
          code={TABLE_CODE}
          collapsible
          defaultOpen={false}
          filename="Table.tsx"
          language="tsx"
        />
      </section>
    </div>
  );
}
