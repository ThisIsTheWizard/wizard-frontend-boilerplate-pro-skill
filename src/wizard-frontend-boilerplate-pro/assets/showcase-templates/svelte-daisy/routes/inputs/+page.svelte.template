<script lang="ts">
  import { CodeBlock } from "$lib/components/ui/code-block";

  let plan = $state("free");

  const BUTTON_CODE = `<div class="flex flex-col gap-2">
  <div class="flex flex-wrap gap-2">
    <button class="btn btn-primary" type="button">Primary</button>
    <button class="btn btn-outline btn-primary" type="button">Outline</button>
    <button class="btn btn-secondary" type="button">Secondary</button>
    <button class="btn btn-error" type="button">Error</button>
    <button class="btn btn-ghost" type="button">Ghost</button>
  </div>
  <div class="flex flex-wrap items-center gap-2">
    <button class="btn btn-primary btn-sm" type="button">Small</button>
    <button class="btn btn-primary" type="button">Default</button>
    <button class="btn btn-primary btn-lg" type="button">Large</button>
    <button class="btn btn-primary btn-disabled" disabled type="button">Disabled</button>
  </div>
</div>`;

  const INPUT_CODE = `<div class="flex flex-col gap-3" style="max-width: 360px">
  <label class="form-control w-full">
    <div class="label"><span class="label-text">Email address</span></div>
    <input class="input input-bordered w-full" placeholder="name@example.com" type="email" />
    <div class="label">
      <span class="label-text-alt text-base-content/60">We'll never share your email.</span>
    </div>
  </label>
  <label class="form-control w-full">
    <div class="label"><span class="label-text">Username</span></div>
    <input class="input input-bordered input-error w-full" value="invalid user!" type="text" />
    <div class="label">
      <span class="label-text-alt text-error">Please enter a valid username.</span>
    </div>
  </label>
</div>`;

  const TEXTAREA_CODE = `<label class="form-control w-full" style="max-width: 360px">
  <div class="label"><span class="label-text">Bio</span></div>
  <textarea class="textarea textarea-bordered w-full" placeholder="Tell us about yourself…" rows="4"></textarea>
  <div class="label">
    <span class="label-text-alt text-base-content/60">Describe your project in a few sentences.</span>
  </div>
</label>`;

  const SELECT_CODE = `<label class="form-control w-full" style="max-width: 360px">
  <div class="label"><span class="label-text">Framework</span></div>
  <select class="select select-bordered w-full">
    <option disabled value="">Select a framework…</option>
    <option value="nextjs">Next.js</option>
    <option value="nuxt">Nuxt</option>
    <option value="sveltekit">SvelteKit</option>
    <option value="astro">Astro</option>
  </select>
</label>`;

  const CHECKBOX_CODE = `<div class="flex flex-col gap-3">
  <label class="flex items-center gap-3 cursor-pointer">
    <input class="checkbox checkbox-primary" type="checkbox" />
    <span class="label-text">Accept terms and conditions</span>
  </label>
  <label class="flex items-center gap-3 cursor-pointer">
    <input class="checkbox checkbox-primary" checked type="checkbox" />
    <span class="label-text">Checked by default</span>
  </label>
  <label class="flex items-center gap-3 cursor-pointer opacity-50">
    <input class="checkbox" disabled type="checkbox" />
    <span class="label-text">Disabled</span>
  </label>
</div>`;

  const RADIO_CODE = `<script lang="ts">
  let plan = $state("free");
<\/script>

<div class="flex flex-col gap-2">
  <span class="label-text font-medium">Plan</span>
  {#each ["free", "pro", "enterprise"] as value}
    <label class="flex items-center gap-3 cursor-pointer">
      <input bind:group={plan} class="radio radio-primary" name="plan" type="radio" {value} />
      <span class="label-text capitalize">{value}</span>
    </label>
  {/each}
</div>`;

  const TOGGLE_CODE = `<div class="flex flex-col gap-3">
  <label class="flex items-center gap-3 cursor-pointer">
    <input class="toggle toggle-primary" type="checkbox" />
    <span class="label-text">Email notifications</span>
  </label>
  <label class="flex items-center gap-3 cursor-pointer">
    <input class="toggle toggle-primary" checked type="checkbox" />
    <span class="label-text">Marketing emails</span>
  </label>
  <label class="flex items-center gap-3 cursor-pointer opacity-50">
    <input class="toggle" disabled type="checkbox" />
    <span class="label-text">Disabled</span>
  </label>
</div>`;
</script>

<div class="flex flex-col gap-4">
  <div>
    <div class="badge badge-outline badge-sm gap-1 uppercase tracking-wider text-xs font-medium mb-3" style="border-color: oklch(var(--p)/0.3); color: oklch(var(--p)); background: oklch(var(--p)/0.08)">Components</div>
    <h1 class="text-4xl font-bold mb-2">Inputs</h1>
    <p class="text-base-content opacity-70 max-w-2xl text-base">
      Form controls for collecting user input — buttons, text fields, selections, and toggles.
    </p>
  </div>

  <!-- Button -->
  <section id="button" class="mb-2">
    <div class="mb-2 border-l-2 border-primary pl-4">
      <h2 class="text-xl font-semibold mb-1">Button</h5>
      <p class="text-base-content/60 text-sm mb-0">
        DaisyUI's <code class="kbd kbd-sm">btn</code> with colour modifiers, size modifiers,
        and disabled state. No import needed — purely CSS classes.
      </p>
    </div>
    <div class="rounded-xl border border-base-300 bg-base-200/60 p-8 shadow-sm mb-3">
      <div class="flex flex-col gap-2">
        <div class="flex flex-wrap gap-2">
          <button class="btn btn-primary" type="button">Primary</button>
          <button class="btn btn-outline btn-primary" type="button">Outline</button>
          <button class="btn btn-secondary" type="button">Secondary</button>
          <button class="btn btn-error" type="button">Error</button>
          <button class="btn btn-ghost" type="button">Ghost</button>
        </div>
        <div class="flex flex-wrap items-center gap-2">
          <button class="btn btn-primary btn-sm" type="button">Small</button>
          <button class="btn btn-primary" type="button">Default</button>
          <button class="btn btn-primary btn-lg" type="button">Large</button>
          <button class="btn btn-primary btn-disabled" disabled type="button">Disabled</button>
        </div>
      </div>
    </div>
    <CodeBlock code={BUTTON_CODE} collapsible defaultOpen={false} filename="Button.svelte" language="svelte" />
  </section>

  <div class="divider my-2"></div>

  <!-- Input -->
  <section id="input" class="mb-2">
    <div class="mb-2 border-l-2 border-primary pl-4">
      <h2 class="text-xl font-semibold mb-1">Input</h5>
      <p class="text-base-content/60 text-sm mb-0">
        Text field using <code class="kbd kbd-sm">input input-bordered</code>.
        Wrap in <code class="kbd kbd-sm">form-control</code> for label/helper slots.
        Add <code class="kbd kbd-sm">input-error</code> for validation.
      </p>
    </div>
    <div class="rounded-xl border border-base-300 bg-base-200/60 p-8 shadow-sm mb-3">
      <div class="flex flex-col gap-3" style="max-width: 360px">
        <label class="form-control w-full">
          <div class="label"><span class="label-text">Email address</span></div>
          <input class="input input-bordered w-full" placeholder="name@example.com" type="email" />
          <div class="label">
            <span class="label-text-alt text-base-content/60">We'll never share your email.</span>
          </div>
        </label>
        <label class="form-control w-full">
          <div class="label"><span class="label-text">Username</span></div>
          <input class="input input-bordered input-error w-full" value="invalid user!" type="text" />
          <div class="label">
            <span class="label-text-alt text-error">Please enter a valid username.</span>
          </div>
        </label>
        <label class="form-control w-full">
          <div class="label"><span class="label-text">Disabled</span></div>
          <input class="input input-bordered w-full" disabled placeholder="Not editable" type="text" />
        </label>
      </div>
    </div>
    <CodeBlock code={INPUT_CODE} collapsible defaultOpen={false} filename="Input.svelte" language="svelte" />
  </section>

  <div class="divider my-2"></div>

  <!-- Textarea -->
  <section id="textarea" class="mb-2">
    <div class="mb-2 border-l-2 border-primary pl-4">
      <h2 class="text-xl font-semibold mb-1">Textarea</h5>
      <p class="text-base-content/60 text-sm mb-0">
        Multi-line input using <code class="kbd kbd-sm">textarea textarea-bordered</code>.
        Add <code class="kbd kbd-sm">textarea-error</code> for validation.
      </p>
    </div>
    <div class="rounded-xl border border-base-300 bg-base-200/60 p-8 shadow-sm mb-3">
      <div class="flex flex-col gap-3" style="max-width: 360px">
        <label class="form-control w-full">
          <div class="label"><span class="label-text">Bio</span></div>
          <textarea class="textarea textarea-bordered w-full" placeholder="Tell us about yourself…" rows="4"></textarea>
          <div class="label">
            <span class="label-text-alt text-base-content/60">Describe your project in a few sentences.</span>
          </div>
        </label>
        <label class="form-control w-full">
          <div class="label"><span class="label-text">Message</span></div>
          <textarea class="textarea textarea-bordered textarea-error w-full" placeholder="Enter your message…" rows="3"></textarea>
          <div class="label">
            <span class="label-text-alt text-error">Message is required.</span>
          </div>
        </label>
      </div>
    </div>
    <CodeBlock code={TEXTAREA_CODE} collapsible defaultOpen={false} filename="Textarea.svelte" language="svelte" />
  </section>

  <div class="divider my-2"></div>

  <!-- Select -->
  <section id="select" class="mb-2">
    <div class="mb-2 border-l-2 border-primary pl-4">
      <h2 class="text-xl font-semibold mb-1">Select</h5>
      <p class="text-base-content/60 text-sm mb-0">
        Native <code class="kbd kbd-sm">&lt;select&gt;</code> styled with
        <code class="kbd kbd-sm">select select-bordered</code>.
      </p>
    </div>
    <div class="rounded-xl border border-base-300 bg-base-200/60 p-8 shadow-sm mb-3">
      <div class="flex flex-col gap-3" style="max-width: 360px">
        <label class="form-control w-full">
          <div class="label"><span class="label-text">Framework</span></div>
          <select class="select select-bordered w-full">
            <option disabled value="">Select a framework…</option>
            <option value="nextjs">Next.js</option>
            <option value="nuxt">Nuxt</option>
            <option value="sveltekit">SvelteKit</option>
            <option value="astro">Astro</option>
          </select>
        </label>
        <label class="form-control w-full">
          <div class="label"><span class="label-text">Disabled</span></div>
          <select class="select select-bordered w-full" disabled>
            <option value="">None</option>
          </select>
        </label>
      </div>
    </div>
    <CodeBlock code={SELECT_CODE} collapsible defaultOpen={false} filename="Select.svelte" language="svelte" />
  </section>

  <div class="divider my-2"></div>

  <!-- Checkbox -->
  <section id="checkbox" class="mb-2">
    <div class="mb-2 border-l-2 border-primary pl-4">
      <h2 class="text-xl font-semibold mb-1">Checkbox</h5>
      <p class="text-base-content/60 text-sm mb-0">
        Binary toggle using <code class="kbd kbd-sm">checkbox checkbox-primary</code>.
      </p>
    </div>
    <div class="rounded-xl border border-base-300 bg-base-200/60 p-8 shadow-sm mb-3">
      <div class="flex flex-col gap-3">
        <label class="flex items-center gap-3 cursor-pointer">
          <input class="checkbox checkbox-primary" type="checkbox" />
          <span class="label-text">Accept terms and conditions</span>
        </label>
        <label class="flex items-center gap-3 cursor-pointer">
          <input class="checkbox checkbox-primary" checked type="checkbox" />
          <span class="label-text">Checked by default</span>
        </label>
        <label class="flex items-center gap-3 cursor-pointer opacity-50">
          <input class="checkbox" disabled type="checkbox" />
          <span class="label-text">Disabled</span>
        </label>
      </div>
    </div>
    <CodeBlock code={CHECKBOX_CODE} collapsible defaultOpen={false} filename="Checkbox.svelte" language="svelte" />
  </section>

  <div class="divider my-2"></div>

  <!-- RadioGroup -->
  <section id="radio-group" class="mb-2">
    <div class="mb-2 border-l-2 border-primary pl-4">
      <h2 class="text-xl font-semibold mb-1">RadioGroup</h5>
      <p class="text-base-content/60 text-sm mb-0">
        Mutually exclusive options using <code class="kbd kbd-sm">radio radio-primary</code>
        bound with <code class="kbd kbd-sm">bind:group</code>.
      </p>
    </div>
    <div class="rounded-xl border border-base-300 bg-base-200/60 p-8 shadow-sm mb-3">
      <div class="flex flex-col gap-2">
        <span class="label-text font-medium">Plan</span>
        {#each ["free", "pro", "enterprise"] as value}
          <label class="flex items-center gap-3 cursor-pointer">
            <input bind:group={plan} class="radio radio-primary" name="plan-demo" type="radio" {value} />
            <span class="label-text capitalize">{value}</span>
          </label>
        {/each}
      </div>
    </div>
    <CodeBlock code={RADIO_CODE} collapsible defaultOpen={false} filename="RadioGroup.svelte" language="svelte" />
  </section>

  <div class="divider my-2"></div>

  <!-- Toggle -->
  <section id="toggle" class="mb-2">
    <div class="mb-2 border-l-2 border-primary pl-4">
      <h2 class="text-xl font-semibold mb-1">Toggle</h5>
      <p class="text-base-content/60 text-sm mb-0">
        On/off slide control using <code class="kbd kbd-sm">toggle toggle-primary</code>.
      </p>
    </div>
    <div class="rounded-xl border border-base-300 bg-base-200/60 p-8 shadow-sm mb-3">
      <div class="flex flex-col gap-3">
        <label class="flex items-center gap-3 cursor-pointer">
          <input class="toggle toggle-primary" type="checkbox" />
          <span class="label-text">Email notifications</span>
        </label>
        <label class="flex items-center gap-3 cursor-pointer">
          <input class="toggle toggle-primary" checked type="checkbox" />
          <span class="label-text">Marketing emails</span>
        </label>
        <label class="flex items-center gap-3 cursor-pointer opacity-50">
          <input class="toggle" disabled type="checkbox" />
          <span class="label-text">Disabled</span>
        </label>
      </div>
    </div>
    <CodeBlock code={TOGGLE_CODE} collapsible defaultOpen={false} filename="Toggle.svelte" language="svelte" />
  </section>
</div>
