"use client";

import { useState } from "react";
import {
  Button,
  Checkbox,
  Input,
  Radio,
  RadioGroup,
  Select,
  SelectItem,
  Switch,
  Textarea,
} from "@heroui/react";

import { CodeBlock } from "@/components/ui/code-block";

const BUTTON_CODE = `import { Button } from "@heroui/react";

export function Example() {
  return (
    <div className="flex flex-wrap gap-2">
      <Button color="primary">Primary</Button>
      <Button color="primary" variant="bordered">Bordered</Button>
      <Button color="primary" variant="light">Light</Button>
      <Button color="primary" variant="flat">Flat</Button>
      <Button color="primary" variant="ghost">Ghost</Button>
      <Button color="danger">Danger</Button>
      <Button isDisabled>Disabled</Button>
    </div>
  );
}`;

const INPUT_CODE = `import { Input } from "@heroui/react";

export function Example() {
  return (
    <Input
      label="Email address"
      placeholder="you@example.com"
      type="email"
      description="We'll never share your email."
    />
  );
}`;

const TEXTAREA_CODE = `import { Textarea } from "@heroui/react";

export function Example() {
  return (
    <Textarea
      label="Bio"
      placeholder="Tell us about yourself…"
      description="Describe your project in a few sentences."
      minRows={4}
    />
  );
}`;

const SELECT_CODE = `import { Select, SelectItem } from "@heroui/react";

const frameworks = ["Next.js", "Remix", "Astro", "SvelteKit"];

export function Example() {
  return (
    <Select label="Framework" placeholder="Select a framework">
      {frameworks.map((fw) => (
        <SelectItem key={fw}>{fw}</SelectItem>
      ))}
    </Select>
  );
}`;

const CHECKBOX_CODE = `import { Checkbox } from "@heroui/react";

export function Example() {
  return (
    <div className="flex flex-col gap-2">
      <Checkbox defaultSelected color="primary">Accept terms and conditions</Checkbox>
      <Checkbox color="primary">Subscribe to newsletter</Checkbox>
      <Checkbox isDisabled>Disabled option</Checkbox>
    </div>
  );
}`;

const RADIO_GROUP_CODE = `import { Radio, RadioGroup } from "@heroui/react";

export function Example() {
  return (
    <RadioGroup label="Plan" color="primary" defaultValue="free">
      <Radio value="free">Free</Radio>
      <Radio value="pro">Pro</Radio>
      <Radio value="enterprise">Enterprise</Radio>
    </RadioGroup>
  );
}`;

const SWITCH_CODE = `import { Switch } from "@heroui/react";

export function Example() {
  return (
    <div className="flex flex-col gap-2">
      <Switch defaultSelected color="primary">Email notifications</Switch>
      <Switch color="primary">Marketing emails</Switch>
      <Switch isDisabled>Disabled</Switch>
    </div>
  );
}`;

const FRAMEWORKS = ["Next.js", "Remix", "Astro", "SvelteKit"];

export default function InputsPage() {
  const [termsChecked, setTermsChecked] = useState(false);
  const [newsletterChecked, setNewsletterChecked] = useState(false);
  const [plan, setPlan] = useState("free");
  const [notifications, setNotifications] = useState(false);
  const [marketing, setMarketing] = useState(true);

  return (
    <div className="flex flex-col gap-16">
      <div>
        <h1 className="text-3xl font-bold tracking-tight text-foreground">
          Inputs
        </h1>
        <p className="mt-2 text-muted-foreground">
          Form controls built with HeroUI — fully accessible components powered
          by React Aria and styled with Tailwind CSS.
        </p>
      </div>

      <section className="flex flex-col gap-4" id="button">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Button</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            HeroUI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Button</code>{" "}
            — use the{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">color</code>{" "}
            prop for colour variants and{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">variant</code>{" "}
            for appearance (solid, bordered, light, flat, ghost, shadow).
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex flex-col gap-4">
            <div className="flex flex-wrap gap-2">
              <Button color="primary">Primary</Button>
              <Button color="primary" variant="bordered">Bordered</Button>
              <Button color="primary" variant="light">Light</Button>
              <Button color="primary" variant="flat">Flat</Button>
              <Button color="primary" variant="ghost">Ghost</Button>
              <Button color="danger">Danger</Button>
              <Button isDisabled>Disabled</Button>
            </div>
            <div className="flex flex-wrap items-center gap-2">
              <Button color="primary" size="sm">Small</Button>
              <Button color="primary" size="md">Medium</Button>
              <Button color="primary" size="lg">Large</Button>
              <Button color="primary" isLoading>Loading</Button>
            </div>
          </div>
        </div>
        <CodeBlock
          code={BUTTON_CODE}
          collapsible
          defaultOpen={false}
          filename="Button.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="input">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Input</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            HeroUI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Input</code>{" "}
            — built-in{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">label</code>,{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">description</code>, and{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">errorMessage</code>{" "}
            props. Use{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">isInvalid</code>{" "}
            for error state.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex max-w-sm flex-col gap-4">
            <Input
              description="We'll never share your email."
              label="Email address"
              placeholder="you@example.com"
              type="email"
            />
            <Input
              errorMessage="Please enter a valid username."
              isInvalid
              label="Username"
              defaultValue="invalid user!"
            />
            <Input
              isDisabled
              label="Disabled"
              placeholder="Not editable"
            />
          </div>
        </div>
        <CodeBlock
          code={INPUT_CODE}
          collapsible
          defaultOpen={false}
          filename="Input.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="textarea">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Textarea</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            HeroUI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Textarea</code>{" "}
            — multi-line text input with the same{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">label</code>{" "}
            and{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">description</code>{" "}
            API as Input. Supports{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">minRows</code>{" "}
            and{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">maxRows</code>{" "}
            for auto-resize.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex max-w-sm flex-col gap-4">
            <Textarea
              description="Describe your project in a few sentences."
              label="Bio"
              minRows={4}
              placeholder="Tell us about yourself…"
            />
            <Textarea
              errorMessage="Message is required."
              isInvalid
              label="Message"
              placeholder="Enter your message…"
            />
          </div>
        </div>
        <CodeBlock
          code={TEXTAREA_CODE}
          collapsible
          defaultOpen={false}
          filename="Textarea.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="select">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Select</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            HeroUI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Select</code>{" "}
            +{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">SelectItem</code>{" "}
            — a fully accessible select menu with keyboard navigation and ARIA.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="max-w-sm">
            <Select label="Framework" placeholder="Select a framework">
              {FRAMEWORKS.map((fw) => (
                <SelectItem key={fw}>{fw}</SelectItem>
              ))}
            </Select>
          </div>
        </div>
        <CodeBlock
          code={SELECT_CODE}
          collapsible
          defaultOpen={false}
          filename="Select.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="checkbox">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Checkbox</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            HeroUI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Checkbox</code>{" "}
            — controlled via{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">isSelected</code>{" "}
            +{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">onValueChange</code>{" "}
            or uncontrolled via{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">defaultSelected</code>.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex flex-col gap-2">
            <Checkbox
              color="primary"
              isSelected={termsChecked}
              onValueChange={setTermsChecked}
            >
              Accept terms and conditions
            </Checkbox>
            <Checkbox
              color="primary"
              isSelected={newsletterChecked}
              onValueChange={setNewsletterChecked}
            >
              Subscribe to newsletter
            </Checkbox>
            <Checkbox isDisabled>Disabled option</Checkbox>
          </div>
        </div>
        <CodeBlock
          code={CHECKBOX_CODE}
          collapsible
          defaultOpen={false}
          filename="Checkbox.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="radio-group">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">RadioGroup</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            HeroUI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">RadioGroup</code>{" "}
            +{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Radio</code>{" "}
            — arrow-key navigation, built-in label, and{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">color</code>{" "}
            theming.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <RadioGroup
            color="primary"
            label="Plan"
            value={plan}
            onValueChange={setPlan}
          >
            <Radio value="free">Free</Radio>
            <Radio value="pro">Pro</Radio>
            <Radio value="enterprise">Enterprise</Radio>
          </RadioGroup>
        </div>
        <CodeBlock
          code={RADIO_GROUP_CODE}
          collapsible
          defaultOpen={false}
          filename="RadioGroup.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="switch">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Switch</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            HeroUI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Switch</code>{" "}
            — controlled via{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">isSelected</code>{" "}
            +{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">onValueChange</code>.
            Supports{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">color</code>,{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">size</code>, and{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">isDisabled</code>.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="flex flex-col gap-2">
            <Switch
              color="primary"
              isSelected={notifications}
              onValueChange={setNotifications}
            >
              Email notifications
            </Switch>
            <Switch
              color="primary"
              isSelected={marketing}
              onValueChange={setMarketing}
            >
              Marketing emails
            </Switch>
            <Switch isDisabled>Disabled</Switch>
          </div>
        </div>
        <CodeBlock
          code={SWITCH_CODE}
          collapsible
          defaultOpen={false}
          filename="Switch.tsx"
          language="tsx"
        />
      </section>
    </div>
  );
}
