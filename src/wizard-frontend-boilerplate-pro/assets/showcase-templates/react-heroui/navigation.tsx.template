"use client";

import { useState } from "react";
import {
  BreadcrumbItem,
  Breadcrumbs,
  Pagination,
  Tab,
  Tabs,
} from "@heroui/react";

import { CodeBlock } from "@/components/ui/code-block";

const TABS_CODE = `import { Tabs, Tab } from "@heroui/react";

export function Example() {
  return (
    <Tabs color="primary" aria-label="Settings tabs">
      <Tab key="account" title="Account">
        <p className="mt-2 text-sm text-muted-foreground">
          Manage your account settings and preferences.
        </p>
      </Tab>
      <Tab key="billing" title="Billing">
        <p className="mt-2 text-sm text-muted-foreground">
          Manage your billing information and subscription.
        </p>
      </Tab>
      <Tab key="security" title="Security">
        <p className="mt-2 text-sm text-muted-foreground">
          Configure two-factor authentication and sessions.
        </p>
      </Tab>
    </Tabs>
  );
}`;

const BREADCRUMB_CODE = `import { Breadcrumbs, BreadcrumbItem } from "@heroui/react";

export function Example() {
  return (
    <Breadcrumbs>
      <BreadcrumbItem href="/">Home</BreadcrumbItem>
      <BreadcrumbItem href="/settings">Settings</BreadcrumbItem>
      <BreadcrumbItem>Profile</BreadcrumbItem>
    </Breadcrumbs>
  );
}`;

const PAGINATION_CODE = `import { Pagination } from "@heroui/react";
import { useState } from "react";

export function Example() {
  const [page, setPage] = useState(1);

  return (
    <Pagination
      total={10}
      page={page}
      onChange={setPage}
      color="primary"
      showControls
    />
  );
}`;

export default function NavigationPage() {
  const [page, setPage] = useState(1);

  return (
    <div className="flex flex-col gap-16">
      <div>
        <h1 className="text-3xl font-bold tracking-tight text-foreground">
          Navigation
        </h1>
        <p className="mt-2 text-muted-foreground">
          Navigational components — HeroUI provides fully accessible{" "}
          <code className="rounded bg-muted px-1 py-0.5 text-xs">Tabs</code>,{" "}
          <code className="rounded bg-muted px-1 py-0.5 text-xs">Breadcrumbs</code>, and{" "}
          <code className="rounded bg-muted px-1 py-0.5 text-xs">Pagination</code>.
        </p>
      </div>

      <section className="flex flex-col gap-4" id="tabs">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Tabs</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            HeroUI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Tabs</code>{" "}
            +{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Tab</code>{" "}
            — keyboard navigable with{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">color</code>{" "}
            theming and{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">variant</code>{" "}
            (solid, underlined, bordered, light).
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <div className="max-w-md">
            <Tabs aria-label="Settings tabs" color="primary">
              <Tab key="account" title="Account">
                <p className="mt-2 text-sm text-muted-foreground">
                  Manage your account settings and preferences.
                </p>
              </Tab>
              <Tab key="billing" title="Billing">
                <p className="mt-2 text-sm text-muted-foreground">
                  Manage your billing information and subscription.
                </p>
              </Tab>
              <Tab key="security" title="Security">
                <p className="mt-2 text-sm text-muted-foreground">
                  Configure two-factor authentication and sessions.
                </p>
              </Tab>
              <Tab key="notifications" title="Notifications">
                <p className="mt-2 text-sm text-muted-foreground">
                  Control how and when you receive notifications.
                </p>
              </Tab>
            </Tabs>
          </div>
        </div>
        <CodeBlock
          code={TABS_CODE}
          collapsible
          defaultOpen={false}
          filename="Tabs.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="breadcrumb">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Breadcrumb</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            HeroUI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Breadcrumbs</code>{" "}
            +{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">BreadcrumbItem</code>{" "}
            — use the{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">href</code>{" "}
            prop to make items links. The last item is automatically styled as the
            current page.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <Breadcrumbs>
            <BreadcrumbItem href="/">Home</BreadcrumbItem>
            <BreadcrumbItem href="/library">Library</BreadcrumbItem>
            <BreadcrumbItem href="/library/navigation">Navigation</BreadcrumbItem>
            <BreadcrumbItem>Breadcrumb</BreadcrumbItem>
          </Breadcrumbs>
        </div>
        <CodeBlock
          code={BREADCRUMB_CODE}
          collapsible
          defaultOpen={false}
          filename="Breadcrumb.tsx"
          language="tsx"
        />
      </section>

      <section className="flex flex-col gap-4" id="pagination">
        <div className="border-l-2 border-primary/40 pl-3">
          <h2 className="text-xl font-semibold text-foreground">Pagination</h2>
          <p className="mt-1 text-sm text-muted-foreground">
            HeroUI{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">Pagination</code>{" "}
            — pass{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">total</code>{" "}
            and bind{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">page</code>{" "}
            +{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">onChange</code>.{" "}
            Use{" "}
            <code className="rounded bg-muted px-1 py-0.5 text-xs">showControls</code>{" "}
            for Previous/Next arrows.
          </p>
        </div>
        <div className="rounded-lg border border-border bg-muted/50 p-8">
          <Pagination
            color="primary"
            page={page}
            showControls
            total={10}
            onChange={setPage}
          />
        </div>
        <CodeBlock
          code={PAGINATION_CODE}
          collapsible
          defaultOpen={false}
          filename="Pagination.tsx"
          language="tsx"
        />
      </section>
    </div>
  );
}
