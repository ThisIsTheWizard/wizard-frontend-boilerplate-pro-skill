<script lang="ts">
  import { toast, Toaster } from "svelte-sonner";

  import { Alert, AlertDescription, AlertTitle } from "$lib/components/ui/alert";
  import { Button } from "$lib/components/ui/button";
  import { CodeBlock } from "$lib/components/ui/code-block";
  import {
    Dialog,
    DialogClose,
    DialogContent,
    DialogDescription,
    DialogFooter,
    DialogHeader,
    DialogTitle,
    DialogTrigger,
  } from "$lib/components/ui/dialog";
  import { Progress } from "$lib/components/ui/progress";
  import { Tooltip } from "$lib/components/ui/tooltip";

  const ALERT_CODE = `<script lang="ts">
  import { Alert, AlertDescription, AlertTitle } from "$lib/components/ui/alert";
<\/script>

<div class="flex flex-col gap-3">
  <Alert>
    <AlertTitle>Heads up</AlertTitle>
    <AlertDescription>
      You can add components to your app using the CLI.
    </AlertDescription>
  </Alert>
  <Alert variant="success">
    <AlertTitle>Success</AlertTitle>
    <AlertDescription>Your changes have been saved.</AlertDescription>
  </Alert>
  <Alert variant="warning">
    <AlertTitle>Warning</AlertTitle>
    <AlertDescription>Your subscription expires in 3 days.</AlertDescription>
  </Alert>
  <Alert variant="destructive">
    <AlertTitle>Error</AlertTitle>
    <AlertDescription>Something went wrong. Please try again.</AlertDescription>
  </Alert>
</div>`;

  const DIALOG_CODE = `<script lang="ts">
  import { Button } from "$lib/components/ui/button";
  import {
    Dialog,
    DialogClose,
    DialogContent,
    DialogDescription,
    DialogFooter,
    DialogHeader,
    DialogTitle,
    DialogTrigger,
  } from "$lib/components/ui/dialog";

  let open = $state(false);
<\/script>

<Dialog onOpenChange={(val) => (open = val)} {open}>
  <DialogTrigger>
    <Button>Open dialog</Button>
  </DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Confirm action</DialogTitle>
      <DialogDescription>
        This action cannot be undone. Are you sure you want to proceed?
      </DialogDescription>
    </DialogHeader>
    <DialogFooter>
      <DialogClose>
        <Button variant="outline">Cancel</Button>
      </DialogClose>
      <Button onclick={() => (open = false)}>Confirm</Button>
    </DialogFooter>
  </DialogContent>
</Dialog>`;

  const PROGRESS_CODE = `<script lang="ts">
  import { Progress } from "$lib/components/ui/progress";
<\/script>

<div class="flex flex-col gap-4">
  <Progress label="25%" value={25} />
  <Progress label="60%" value={60} />
  <Progress label="100%" value={100} />
  <Progress indeterminate label="Loading…" />
</div>`;

  const TOAST_CODE = `<script lang="ts">
  import { toast, Toaster } from "svelte-sonner";
  import { Button } from "$lib/components/ui/button";
<\/script>

<!-- Place <Toaster /> once in your root layout -->
<Toaster />

<div class="flex flex-wrap gap-2">
  <Button onclick={() => toast("Event has been created.")}>Default</Button>
  <Button onclick={() => toast.success("Profile saved successfully.")} variant="outline">
    Success
  </Button>
  <Button onclick={() => toast.error("Something went wrong.")} variant="outline">
    Error
  </Button>
  <Button onclick={() => toast.warning("Unsaved changes detected.")} variant="outline">
    Warning
  </Button>
</div>`;

  const TOOLTIP_CODE = `<script lang="ts">
  import { Button } from "$lib/components/ui/button";
  import { Tooltip } from "$lib/components/ui/tooltip";
<\/script>

<div class="flex flex-wrap items-center gap-4">
  <Tooltip content="Tooltip on top" side="top">
    <Button variant="outline">Top</Button>
  </Tooltip>
  <Tooltip content="Tooltip on right" side="right">
    <Button variant="outline">Right</Button>
  </Tooltip>
  <Tooltip content="Tooltip on bottom" side="bottom">
    <Button variant="outline">Bottom</Button>
  </Tooltip>
  <Tooltip content="Tooltip on left" side="left">
    <Button variant="outline">Left</Button>
  </Tooltip>
</div>`;

  let dialogOpen = $state(false);
</script>

<div class="flex flex-col gap-16">
  <div>
    <h1 class="text-3xl font-bold tracking-tight text-foreground">Feedback</h1>
    <p class="mt-2 text-muted-foreground">
      Status indicators, notifications, and modal interactions — alerts, toasts,
      dialogs, and progress.
    </p>
  </div>

  <section class="flex flex-col gap-4" id="alert">
    <div>
      <h2 class="text-xl font-semibold text-foreground">Alert</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        An inline message banner for default, success, warning, and destructive
        states.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-card p-8">
      <div class="flex flex-col gap-3">
        <Alert>
          <AlertTitle>Heads up</AlertTitle>
          <AlertDescription>
            You can add components to your app using the CLI.
          </AlertDescription>
        </Alert>
        <Alert variant="success">
          <AlertTitle>Success</AlertTitle>
          <AlertDescription>Your changes have been saved.</AlertDescription>
        </Alert>
        <Alert variant="warning">
          <AlertTitle>Warning</AlertTitle>
          <AlertDescription>Your subscription expires in 3 days.</AlertDescription>
        </Alert>
        <Alert variant="destructive">
          <AlertTitle>Error</AlertTitle>
          <AlertDescription>Something went wrong. Please try again.</AlertDescription>
        </Alert>
      </div>
    </div>
    <CodeBlock
      code={ALERT_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="Alert.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="toast">
    <div>
      <h2 class="text-xl font-semibold text-foreground">Toast</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        Transient notification messages powered by svelte-sonner, with default,
        success, error, and warning variants.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-card p-8">
      <Toaster />
      <div class="flex flex-wrap gap-2">
        <Button onclick={() => toast("Event has been created.")}>Default</Button>
        <Button
          onclick={() => toast.success("Profile saved successfully.")}
          variant="outline"
        >
          Success
        </Button>
        <Button
          onclick={() => toast.error("Something went wrong.")}
          variant="outline"
        >
          Error
        </Button>
        <Button
          onclick={() => toast.warning("Unsaved changes detected.")}
          variant="outline"
        >
          Warning
        </Button>
      </div>
    </div>
    <CodeBlock
      code={TOAST_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="Toast.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="progress">
    <div>
      <h2 class="text-xl font-semibold text-foreground">Progress</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A horizontal bar showing determinate or indeterminate completion status.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-card p-8">
      <div class="flex flex-col gap-4">
        <Progress label="25%" value={25} />
        <Progress label="60%" value={60} />
        <Progress label="100%" value={100} />
        <Progress indeterminate label="Loading…" />
      </div>
    </div>
    <CodeBlock
      code={PROGRESS_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="Progress.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="tooltip">
    <div>
      <h2 class="text-xl font-semibold text-foreground">Tooltip</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A floating label that appears on hover or focus, supporting top, right,
        bottom, and left placement.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-card p-8">
      <div class="flex flex-wrap items-center gap-4">
        <Tooltip content="Tooltip on top" side="top">
          <Button variant="outline">Top</Button>
        </Tooltip>
        <Tooltip content="Tooltip on right" side="right">
          <Button variant="outline">Right</Button>
        </Tooltip>
        <Tooltip content="Tooltip on bottom" side="bottom">
          <Button variant="outline">Bottom</Button>
        </Tooltip>
        <Tooltip content="Tooltip on left" side="left">
          <Button variant="outline">Left</Button>
        </Tooltip>
      </div>
    </div>
    <CodeBlock
      code={TOOLTIP_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="Tooltip.svelte"
      language="svelte"
    />
  </section>

  <section class="flex flex-col gap-4" id="dialog">
    <div>
      <h2 class="text-xl font-semibold text-foreground">Dialog</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        A modal overlay with a trigger, header, body, and footer actions —
        focus-trapped and accessible.
      </p>
    </div>
    <div class="rounded-lg border border-border bg-card p-8">
      <Dialog onOpenChange={(val) => (dialogOpen = val)} open={dialogOpen}>
        <DialogTrigger>
          <Button>Open dialog</Button>
        </DialogTrigger>
        <DialogContent>
          <DialogHeader>
            <DialogTitle>Confirm action</DialogTitle>
            <DialogDescription>
              This action cannot be undone. Are you sure you want to proceed?
            </DialogDescription>
          </DialogHeader>
          <DialogFooter>
            <DialogClose>
              <Button variant="outline">Cancel</Button>
            </DialogClose>
            <Button onclick={() => (dialogOpen = false)}>Confirm</Button>
          </DialogFooter>
        </DialogContent>
      </Dialog>
    </div>
    <CodeBlock
      code={DIALOG_CODE}
      collapsible={true}
      defaultOpen={false}
      filename="Dialog.svelte"
      language="svelte"
    />
  </section>
</div>
